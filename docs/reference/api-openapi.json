{
  "openapi": "3.1.0",
  "info": {
    "title": "samtal-server configuration API",
    "description": "The domain half of one samtal-server deployment's configuration: providers, MCP servers, agents, devices, and the credentials they use. Every request carries a bearer token, whose value is the environment variable `server.api.secret_env` names.\n\nConfiguration is a boot-time snapshot, so a successful write applies at the next server start rather than immediately. Device bindings are the one exception: a running server reads them, and the default agent, as a device asks for them, so binding or unbinding a device applies at that device's next OTA check or connection. The exception ends where the agent does, since a server builds an agent's providers at boot: a binding naming an agent this server has not loaded waits for the restart that loads it. Every write says which of the two happened, in its `notice`.\n\nA device with no binding and no default agent to cover it is answered at its configuration check with a six-digit activation code, which it shows on its screen and speaks. `/devices/pending` lists the devices showing one, keyed by the code, and posting to `/devices/pending/{code}` binds the device showing it. That listing is the running server's own state rather than stored configuration: a restart forgets it, and the devices come back with fresh codes.\n\nThe `/runtime` namespace is the running server's own state as well, and is kept apart from the entity namespaces because an entity may legally be named after any word a route might want. `/runtime/mcp-servers` says what each configured MCP server is doing right now: connected or down, since when, what it published, and which agents may reach it. Nothing there is read from the database, so it cannot disagree with what is running.\n\n`GET /runtime/agents/{name}/prompt` is the other read there: the system prompt a session opening now as that agent would be sent, block by block, with the provenance and the character count of each and the total. It is assembled from the loaded agents, the running MCP slice and the memory store rather than from the database, so it cannot disagree with what a session would get, and it is a preview of a new session rather than a readback of a running one: a conversation already in progress holds the half it assembled at its own activation.\n\n`POST /runtime/mcp-servers/reload` is the second exception to the boot-time snapshot, and unlike device bindings it is asked for rather than noticed. It re-reads the `mcp_servers` entries, the secrets stored on them and the agents' `mcp` grant lists, and applies them to the running server: entries are started, restarted, stopped or left alone, and live conversations pick the result up on their next utterance without being dropped. Everything else about an agent still waits for a restart, which is why writes to those keep saying so.\n\nA provider entry carries whatever options its `type` takes, and those are passed through rather than declared, so no schema can list them. Until typed option models land (#88) they are documented in the example fragments under `samtal-server/examples/`, which is also where the measured numbers behind each default are kept.",
    "version": "1"
  },
  "servers": [
    {
      "url": "/api"
    }
  ],
  "paths": {
    "/config": {
      "get": {
        "summary": "Read Config",
        "description": "The whole domain configuration, masked, with the location of\nevery stored secret beside it.",
        "operationId": "read_config_config_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConfigDocument"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/providers": {
      "get": {
        "summary": "Read Providers",
        "description": "Every provider, by stage and then by name: a provider is\naddressed by the two together, since two stages may hold one\nname.",
        "operationId": "read_providers_providers_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "additionalProperties": {
                      "$ref": "#/components/schemas/Envelope"
                    },
                    "type": "object"
                  },
                  "type": "object",
                  "title": "Response Read Providers Providers Get"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/providers/{stage}/{name}": {
      "get": {
        "summary": "Read Provider",
        "description": "One provider.",
        "operationId": "read_provider_providers__stage___name__get",
        "parameters": [
          {
            "name": "stage",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Stage"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Envelope"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Write Provider",
        "description": "Create or replace one provider from a fragment in the shape\nthe YAML section had.",
        "operationId": "write_provider_providers__stage___name__put",
        "parameters": [
          {
            "name": "stage",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Stage"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProviderConfig"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Provider",
        "description": "Delete one provider, and the secrets stored on it. Refused\nwhile an agent or the agent defaults still name it.",
        "operationId": "remove_provider_providers__stage___name__delete",
        "parameters": [
          {
            "name": "stage",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Stage"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/mcp-servers": {
      "get": {
        "summary": "Read Mcp Servers",
        "description": "Every MCP server, by name.",
        "operationId": "read_mcp_servers_mcp_servers_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/Envelope"
                  },
                  "type": "object",
                  "title": "Response Read Mcp Servers Mcp Servers Get"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/mcp-servers/{name}": {
      "get": {
        "summary": "Read Mcp Server",
        "description": "One MCP server.",
        "operationId": "read_mcp_server_mcp_servers__name__get",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Envelope"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Write Mcp Server",
        "description": "Create or replace one MCP server. The running server applies\nit at the next reload, with no restart.",
        "operationId": "write_mcp_server_mcp_servers__name__put",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/McpServerConfig"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Mcp Server",
        "description": "Delete one MCP server, and the secrets stored on it. The\nrunning server stops it at the next reload.",
        "operationId": "remove_mcp_server_mcp_servers__name__delete",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/agents": {
      "get": {
        "summary": "Read Agents",
        "description": "Every agent, by name.",
        "operationId": "read_agents_agents_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/Envelope"
                  },
                  "type": "object",
                  "title": "Response Read Agents Agents Get"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/agents/{name}": {
      "get": {
        "summary": "Read Agent",
        "description": "One agent.",
        "operationId": "read_agent_agents__name__get",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Envelope"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Write Agent",
        "description": "Create or replace one agent. Every provider and MCP server it\nnames has to exist already, which is what the natural creation\norder is about.",
        "operationId": "write_agent_agents__name__put",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentConfig"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Agent",
        "description": "Delete one agent. Refused while a device binding or the\ndefault agent still names it.",
        "operationId": "remove_agent_agents__name__delete",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/agent-defaults": {
      "get": {
        "summary": "Read Agent Defaults",
        "description": "What every agent uses unless it names something else. One\nentry for the whole deployment, and never missing: an unwritten\none reads as the empty entry.",
        "operationId": "read_agent_defaults_agent_defaults_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Envelope"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Write Agent Defaults",
        "description": "Replace what every agent uses unless it names something else.\nOne entry for the whole deployment, so this is a replace and\nthere is nothing to delete.",
        "operationId": "write_agent_defaults_agent_defaults_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentDefaults"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/devices": {
      "get": {
        "summary": "Read Devices",
        "description": "Every device binding, by the canonical form of its MAC. Bound\ndevices only: the ones waiting to be claimed are the listing\nbelow, since they have no binding to show.",
        "operationId": "read_devices_devices_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/Envelope"
                  },
                  "type": "object",
                  "title": "Response Read Devices Devices Get"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/devices/pending": {
      "get": {
        "summary": "Read Pending Devices",
        "description": "Every device showing an activation code, by the code it is\nshowing.\n\nRuntime state of the server this application is mounted on\nrather than stored configuration, so it touches no database and\ncannot answer the refusals a read of the configuration can. A\nserver that restarts forgets it, and the devices come back\nwithin a couple of minutes with fresh codes.",
        "operationId": "read_pending_devices_devices_pending_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/PendingDevice"
                  },
                  "type": "object",
                  "title": "Response Read Pending Devices Devices Pending Get"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/devices/{mac}": {
      "get": {
        "summary": "Read Device",
        "description": "One device's binding. The MAC is normalized before it is\nlooked up, so `AA-BB-...` and `aa:bb:...` reach the same\ndevice.",
        "operationId": "read_device_devices__mac__get",
        "parameters": [
          {
            "name": "mac",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Mac"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Envelope"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Write Device",
        "description": "Bind one device to one or more agents. The MAC is normalized\nbefore it is written, so the two spellings reach one row.\n\nThe running server reads this binding, so the device meets it at\nits next check-in rather than at the next restart; what the\nacknowledgement says depends on whether the agents named are\nones that server loaded.",
        "operationId": "write_device_devices__mac__put",
        "parameters": [
          {
            "name": "mac",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Mac"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeviceBinding"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Device",
        "description": "Remove one device's binding, which with no default agent set\nmeans the device is refused at the handshake. Live, with no\nagent to be loaded or not: the device stops being served at its\nnext check-in, though a conversation already running is left to\nfinish.",
        "operationId": "remove_device_devices__mac__delete",
        "parameters": [
          {
            "name": "mac",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Mac"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/default-agent": {
      "get": {
        "summary": "Read Default Agent",
        "description": "The agent an unbound device reaches, or null. Unset is a\nconfiguration rather than a missing entity, so this is never a\n404.",
        "operationId": "read_default_agent_default_agent_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefaultAgent"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Write Default Agent",
        "description": "Set the agent an unbound device reaches. Read by the running\nserver the way a binding is, so it applies to the next device\nthat asks unless the agent it names was written since boot.",
        "operationId": "write_default_agent_default_agent_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefaultAgentName"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Default Agent",
        "description": "Unset it, leaving the devices map as the allowlist.\nIdempotent, like the CLI: there is no such thing as a default\nagent that was already not set. Live, like the delete above:\nthe next unbound device to ask is turned away.",
        "operationId": "remove_default_agent_default_agent_delete",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/providers/{stage}/{name}/secrets/{slot}": {
      "put": {
        "summary": "Write Provider Secret",
        "description": "Store one of this provider's credentials, encrypted. The slot\nis the option name the credential fills, such as api_key; a\nstored secret takes precedence over an environment reference\nwritten for the same slot.",
        "operationId": "write_provider_secret_providers__stage___name__secrets__slot__put",
        "parameters": [
          {
            "name": "stage",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Stage"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          },
          {
            "name": "slot",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Slot"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SecretValue"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Provider Secret",
        "description": "Remove one stored credential. A slot holding none is a 404.",
        "operationId": "remove_provider_secret_providers__stage___name__secrets__slot__delete",
        "parameters": [
          {
            "name": "stage",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Stage"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          },
          {
            "name": "slot",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Slot"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/mcp-servers/{name}/secrets/{slot}": {
      "put": {
        "summary": "Write Mcp Secret",
        "description": "Store one of this MCP server's credentials, encrypted. The\nslot is `env.<KEY>` or `headers.<Key>`, which is where the value\nwould otherwise have been written as a $VAR reference.\n\nRotation is exactly what the ciphertext half of the reload's\ndiff applies, so this carries the reload notice too: the entry\nis rebuilt with the fresh credential and reconnected.",
        "operationId": "write_mcp_secret_mcp_servers__name__secrets__slot__put",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          },
          {
            "name": "slot",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Slot"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SecretValue"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Remove Mcp Secret",
        "description": "Remove one stored credential, which the next reload applies\nby rebuilding the entry without it.",
        "operationId": "remove_mcp_secret_mcp_servers__name__secrets__slot__delete",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          },
          {
            "name": "slot",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Slot"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/devices/pending/{code}": {
      "post": {
        "summary": "Add Device",
        "description": "Bind the device showing this activation code, and retire the\ncode.\n\nThe MAC comes from the pending entry, so the operator binds what\nthe board in front of them is showing rather than a MAC they\nhave to go and find. The write itself is the same repository\ncall `PUT /devices/{mac}` makes, so reference checking and\ntransactionality are inherited rather than restated, and the\nacknowledgement is the same one, naming the MAC that was bound.\n\nThe claim is atomic: the code is reserved before the write and\nconsumed after it, so two operators racing one code produce one\nbind and one retryable refusal. A write that fails releases the\nreservation, leaving the code claimable again, because the\ndevice is still showing it.",
        "operationId": "add_device_devices_pending__code__post",
        "parameters": [
          {
            "name": "code",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Code"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeviceBinding"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Acknowledgement"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "Nothing of that identity exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/runtime/mcp-servers": {
      "get": {
        "summary": "Read Mcp Server Status",
        "description": "What each configured MCP server is doing right now.\n\nRuntime state of the server this application is mounted on, like\nthe pending listing: it touches no database, so it can answer\nnone of the refusals a read of the configuration can, and what\nit reports is the slice its managers were built with rather than\nanything read back out of storage.\n\n`async def`, unlike the repository's plain-`def` handlers, on\npurpose: the read then happens on the event loop that owns the\nmanagers, so it cannot interleave with a change to them and\nreport half of one world.\n\nAn application built without a server around it has no runtime\nto report and answers with an empty object, which is the honesty\n`loaded_agents = ()` already has.",
        "operationId": "read_mcp_server_status_runtime_mcp_servers_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "$ref": "#/components/schemas/McpServerStatus"
                  },
                  "type": "object",
                  "title": "Response Read Mcp Server Status Runtime Mcp Servers Get"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    },
    "/runtime/agents/{name}/prompt": {
      "get": {
        "summary": "Read Agent Prompt",
        "description": "The system prompt a session opening now as this agent would\nbe sent, block by block.\n\nA runtime read rather than a database one: it is assembled from\nthe agents this server loaded, the MCP slice its registry is\nrunning and the memory store it writes, so it cannot disagree\nwith what a session would be given, which is the whole point of\nit.\n\nIt is a preview of a new session and says so, because there is\nno honest per-session answer to offer instead: a conversation\nalready in progress holds the know-how half it assembled at its\nown activation, which may predate a reload, and what an operator\naudits is what the configuration produces now.\n\n`async def`, like the status read beside it: the MCP slice is\nread on the loop that mutates it, so the answer cannot be half\nof one world and half of another. The memory read that follows\nis a file read and happens in a worker thread.\n\nAn agent this server did not load is a 404 naming the restart,\nsince an agent is built at boot; an application with no server\naround it answers 503, like the reload.",
        "operationId": "read_agent_prompt_runtime_agents__name__prompt_get",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AssembledPrompt"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "404": {
            "description": "No agent of that name was loaded when this server started. An agent written since then waits for the restart that builds its providers.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "503": {
            "description": "This application has no running server around it, so there is nothing for a runtime action to act on. A deployment reaches the API on its server's own port; an application built without one serves the reads in the /runtime namespace emptily and refuses the actions.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/runtime/mcp-servers/reload": {
      "post": {
        "summary": "Reload Mcp Servers",
        "description": "Re-read the MCP servers and the agents' grants, and apply\nthem to this running server.\n\nThe one action in this namespace, and the one exception to\n\"configuration applies at the next start\" that a request rather\nthan a device asks for. What it re-reads is the `mcp_servers`\nentries, the secrets stored on them and the effective `mcp`\nlists, and nothing else: an agent's prompt, its providers, its\nmemory and the whole server section stay as this process booted\nthem, so a new agent still waits for the restart that builds it.\n\nNothing is stopped or started until every manager the new\nconfiguration needs has been built, so a refusal (422) has\nchanged nothing at all. A server that merely will not connect is\nnot a refusal: it applies, and says `down` with its reason\nbelow, and is reconnected in the background when a session that\nwould use it opens.\n\nLive conversations are not dropped. The tools an agent may reach\nare snapshotted per reply, so a session picks the new world up on\nits next utterance; a call in flight on a server this stopped\nfails into the same error result a server dropping mid-call\nproduces.\n\nOne reload runs at a time: a concurrent one is refused with 409\nand has changed nothing, like a write that could not take the\ndatabase's lock.",
        "operationId": "reload_mcp_servers_runtime_mcp_servers_reload_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/McpReloadResult"
                }
              }
            }
          },
          "401": {
            "description": "The request carried no bearer token, or not the one this server was given.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "409": {
            "description": "Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "The stored configuration was refused: it does not compose into a valid snapshot, or a server it names could not be built (an environment reference nothing sets, a stored credential that will not decrypt, an entry `server.local_only` forbids). Nothing was stopped, started or swapped, and the running servers are exactly as they were.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "503": {
            "description": "This application has no running server around it, so there is nothing for a runtime action to act on. A deployment reaches the API on its server's own port; an application built without one serves the reads in the /runtime namespace emptily and refuses the actions.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Acknowledgement": {
        "properties": {
          "wrote": {
            "type": "string",
            "title": "Wrote",
            "description": "What was written or deleted, naming the entity the way the `samtal-server config` command names it in the line it prints."
          },
          "notice": {
            "type": "string",
            "title": "Notice",
            "description": "When the change takes effect, as one of three sentences. Configuration is a boot-time snapshot, so most writes apply at the next server start. A device binding and the default agent are read by the running server, so they apply at the device's next OTA check or connection with no restart, unless they name an agent this server has not loaded, which is the case that carries the restart sentence again. A write to an MCP server entry or to one of its secret slots names the reload instead, since that is what applies it to a running server."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "wrote",
          "notice"
        ],
        "title": "Acknowledgement",
        "description": "What a write answers with: what it did, and when it takes\neffect."
      },
      "AssembledPrompt": {
        "properties": {
          "blocks": {
            "items": {
              "$ref": "#/components/schemas/PromptBlock"
            },
            "type": "array",
            "title": "Blocks",
            "description": "The blocks in the order they are sent, joined by one blank line each: the persona, then the guidance of each MCP entry the agent is granted in grant order, then the remembered facts. The order is fixed and not configurable. A block that would hold nothing is not sent and is not listed, which is what an agent with no prompt of its own produces."
          },
          "characters": {
            "type": "integer",
            "title": "Characters",
            "description": "The whole prompt's length in characters, which is the sum of the blocks plus the blank line between each pair of them. The prompt is the blocks joined and nothing else, so a character counted here is a character the model receives."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "blocks",
          "characters"
        ],
        "title": "AssembledPrompt",
        "description": "The system prompt a session opening now as this agent would be\nsent."
      },
      "ConfigDocument": {
        "properties": {
          "config": {
            "additionalProperties": true,
            "type": "object",
            "title": "Config",
            "description": "The domain half of the configuration (providers, MCP servers, agent defaults, agents, devices, the default agent) in the shape `docs/reference/domain-config.md` documents, with every secret-bearing value masked."
          },
          "secrets": {
            "items": {
              "$ref": "#/components/schemas/StoredSecretLocation"
            },
            "type": "array",
            "title": "Secrets",
            "description": "Where every secret stored in the database is, which the masked document above cannot say. A list rather than a mapping, because a location is three fields and not a key."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "config",
          "secrets"
        ],
        "title": "ConfigDocument",
        "description": "The whole domain configuration of one deployment, masked."
      },
      "DefaultAgent": {
        "properties": {
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name",
            "description": "The default agent's name, or null when none is set, which leaves the devices map as the allowlist."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name"
        ],
        "title": "DefaultAgent",
        "description": "The agent an unbound device reaches."
      },
      "Envelope": {
        "properties": {
          "entity": {
            "additionalProperties": true,
            "type": "object",
            "title": "Entity",
            "description": "The entity's body in the shape a write of it accepts, with every secret-bearing value masked. Described rather than validated here: a masked value is not one the entity model would accept back, so the entity schemas under `components/schemas` are what say which keys a write may carry."
          },
          "secrets": {
            "additionalProperties": {
              "$ref": "#/components/schemas/SecretSlot"
            },
            "type": "object",
            "title": "Secrets",
            "description": "The slots holding a secret stored in the database, by slot name, and never their values: reads are masked. Empty for the kinds that can hold no stored secret (agents, agent defaults, devices), so that every read has one shape."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "entity",
          "secrets"
        ],
        "title": "Envelope",
        "description": "One entity as a read returns it: the entity, and its stored-secret\nslots beside it."
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "McpReloadResult": {
        "properties": {
          "started": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Started",
            "description": "The entries that had no connection before this reload and have one now: newly written, or newly named by some agent's `mcp` list. Started is not connected: an entry here whose server was unreachable is `down` below, with its reason."
          },
          "restarted": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Restarted",
            "description": "The entries whose fragment or whose stored secrets changed. Their connections were closed and made again, so a rotated credential applies here rather than at the next server start."
          },
          "stopped": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Stopped",
            "description": "The entries this server no longer connects: deleted, or no longer named by any agent. A deleted one is gone from the status below; a de-referenced one is still there, as `unused`."
          },
          "unchanged": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Unchanged",
            "description": "The entries nothing changed about, which kept the connections they had. A reload does not disturb them, so the conversations using them do not notice one."
          },
          "servers": {
            "additionalProperties": {
              "$ref": "#/components/schemas/McpServerStatus"
            },
            "type": "object",
            "title": "Servers",
            "description": "What every configured entry is doing now that the reload has been applied, keyed by entry name: exactly what `GET /runtime/mcp-servers` answers, taken in the same breath so that applying and verifying are one round trip."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "started",
          "restarted",
          "stopped",
          "unchanged",
          "servers"
        ],
        "title": "McpReloadResult",
        "description": "What one reload did, and what is running once it had done it.\n\nBoth halves in one answer on purpose: the request that applies a\nchange is the request that says what the change was and how it came\nout, so believing a write took effect when it did not takes a\ndeliberate act of not reading the reply."
      },
      "McpServerStatus": {
        "properties": {
          "state": {
            "type": "string",
            "enum": [
              "connected",
              "down",
              "unused"
            ],
            "title": "State",
            "description": "What this entry is doing: `connected` and offering the tools below, `down` and offering none, or `unused` because no agent references it, so this server never built a connection for it at all."
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason",
            "description": "Why a `down` server is down, as a fixed token this server owns: the class of the failure, or `DroppedAfterFailedCall` for a connection dropped after a tool call failed on it. Null when it is not down. Never a message the far side wrote, since an MCP server is a third party and its bytes are not this API's to publish."
          },
          "since": {
            "type": "string",
            "title": "Since",
            "description": "When this state was last entered, as an ISO-8601 instant in UTC. A new reason for staying down counts as entering it again, since it is a fresh failure. For an entry no agent references it is when the running configuration took effect."
          },
          "tools": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Tools",
            "description": "What this server published, under the names the model is given (`<entry>__<tool>`, sanitized). Empty while it is down. Only names cross this surface: a description, or the name a server listed before the publishing rule got to it, is bytes that server chose, and a server holding a credential of this deployment's could reflect it in either."
          },
          "grants": {
            "additionalProperties": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": "object",
            "title": "Grants",
            "description": "Which agents may reach this server, by agent name. The value is how much of the server the agent gets: null is the whole server, and a list is the tools that agent was allowed, by the published name without the entry prefix. Beside the published list above it, so an allowed name this server does not offer is answerable in one read."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "state",
          "reason",
          "since",
          "tools",
          "grants"
        ],
        "title": "McpServerStatus",
        "description": "One configured MCP server, as the running server sees it.\n\nThe listing is keyed by the entry's name, because that is what the\noperator wrote and what every tool the server publishes is prefixed\nwith."
      },
      "PendingDevice": {
        "properties": {
          "mac": {
            "type": "string",
            "title": "Mac",
            "description": "The device's MAC in canonical form, which is the row a successful claim writes."
          },
          "client_id": {
            "type": "string",
            "title": "Client Id",
            "description": "The UUID the device sent as its Client-Id at its last check-in."
          },
          "board": {
            "type": "string",
            "title": "Board",
            "description": "The board type the device reported, such as waveshare-esp32-s3-touch-lcd-1.54, or `unknown` when it reported none. Whatever the device said, bounded in length and stripped of anything unprintable."
          },
          "firmware": {
            "type": "string",
            "title": "Firmware",
            "description": "The firmware version the device reported, or 0.0.0 when it reported none."
          },
          "first_seen": {
            "type": "string",
            "title": "First Seen",
            "description": "When this device first checked in, as an ISO-8601 instant in UTC."
          },
          "last_seen": {
            "type": "string",
            "title": "Last Seen",
            "description": "Its most recent check-in, in the same form."
          },
          "expires_at": {
            "type": "string",
            "title": "Expires At",
            "description": "When this code stops being claimable. The device re-checks every couple of minutes and displays whatever the fresh reply carries, so an expired code is replaced on the screen rather than leaving the device stranded."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "mac",
          "client_id",
          "board",
          "firmware",
          "first_seen",
          "last_seen",
          "expires_at"
        ],
        "title": "PendingDevice",
        "description": "One device waiting to be claimed, as the listing shows it.\n\nThe listing is keyed by the code, because the code is what the\noperator has: they are holding a board with six digits on it, and\nthe question the board model and the firmware version answer is\nwhich of these entries is that board."
      },
      "Problem": {
        "properties": {
          "detail": {
            "type": "string",
            "title": "Detail",
            "description": "What was refused and why, the same sentence the `samtal-server config` command prints for it. It names the entity the request addressed and the rule that was broken; it never quotes a secret or a configuration value that was rejected."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "detail"
        ],
        "title": "Problem",
        "description": "A refusal, in the repository's own words."
      },
      "PromptBlock": {
        "properties": {
          "provenance": {
            "type": "string",
            "title": "Provenance",
            "description": "Where this block came from, as a fixed token this server owns: `persona` for the agent's own prompt, `instructions:<entry>` for the guidance written on an MCP server entry the agent is granted, `memory` for what the agent remembers. The entry name is the operator's, and it has been through the rule that makes it a tool-name prefix."
          },
          "characters": {
            "type": "integer",
            "title": "Characters",
            "description": "How long this block is, in characters, counted on what is stored and sent. It is what an operator tunes a small model's context budget against, block by block."
          },
          "text": {
            "type": "string",
            "title": "Text",
            "description": "The block as the model receives it, heading included. A surface that hid part of the prompt would fail its own purpose, which is to say what the model was given."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "provenance",
          "characters",
          "text"
        ],
        "title": "PromptBlock",
        "description": "One block of an assembled system prompt."
      },
      "SecretSlot": {
        "properties": {
          "shadows": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Shadows",
            "description": "The entity key this stored secret displaces, or null when the entity writes no reference for the slot. A stored secret takes precedence over an environment reference written for the same slot, and this names what it takes the place of."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "shadows"
        ],
        "title": "SecretSlot",
        "description": "One slot of an entity that holds a secret stored in the database."
      },
      "StoredSecretLocation": {
        "properties": {
          "kind": {
            "type": "string",
            "title": "Kind",
            "description": "The kind of entity holding it: provider or mcp_server."
          },
          "identity": {
            "type": "string",
            "title": "Identity",
            "description": "The entity's identity: `<stage>.<name>` for a provider, the name for an MCP server."
          },
          "slot": {
            "type": "string",
            "title": "Slot",
            "description": "The credential slot inside that entity."
          },
          "shadows": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Shadows",
            "description": "The entity key this stored secret displaces, or null."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "kind",
          "identity",
          "slot",
          "shadows"
        ],
        "title": "StoredSecretLocation",
        "description": "Where one stored secret is, in the whole-configuration read."
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      },
      "ProviderConfig": {
        "additionalProperties": true,
        "description": "One provider entry. Options beyond `type` are passed through to the\nprovider implementation, so extra keys are allowed here.",
        "properties": {
          "type": {
            "description": "The provider implementation this entry configures, such as anthropic, openai_compatible, faster_whisper, openai, piper, elevenlabs or silero. Every key beyond the ones listed here is an option for that implementation.",
            "minLength": 1,
            "title": "Type",
            "type": "string"
          },
          "api_key_env": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The name of the environment variable holding this provider's credential, never the credential itself. Left unset for a local engine or a keyless self-hosted endpoint. A credential stored with `config set-secret` fills the same slot and takes precedence.",
            "title": "Api Key Env"
          },
          "egress": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Whether this entry sends session data off the host, asserted by the operator for the types whose configuration decides it rather than their name (openai_compatible, and the openai ASR and TTS types, whose base_url may be local or a vendor). Under server.local_only such an entry must declare egress: false; a type that knows its own egress rejects the key.",
            "title": "Egress"
          }
        },
        "required": [
          "type"
        ],
        "title": "ProviderConfig",
        "type": "object"
      },
      "McpServerConfig": {
        "additionalProperties": false,
        "description": "One MCP server, named so agents can reference it.\n\n`transport` decides which of the two field groups applies: a stdio\nserver is a command this server spawns, a streamable_http one is a\nURL it connects to. Naming a field of the other transport is an\nerror rather than a silently ignored key, since the difference\nbetween \"my headers are ignored\" and \"my headers are wrong\" is a\ndebugging afternoon.",
        "properties": {
          "transport": {
            "description": "Which field group applies: stdio spawns `command` as a subprocess, streamable_http connects to `url`. Naming a field of the other transport is an error rather than a silently ignored key. An SSE-only endpoint is configured as a stdio server behind an mcp-proxy bridge, since the specification deprecated SSE in favour of streamable_http and there is no native arm for it here.",
            "enum": [
              "stdio",
              "streamable_http"
            ],
            "title": "Transport",
            "type": "string"
          },
          "command": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The executable a stdio server is spawned as. Required for that transport, and rejected for streamable_http.",
            "title": "Command"
          },
          "args": {
            "description": "The arguments the stdio command is spawned with, one per entry.",
            "items": {
              "type": "string"
            },
            "title": "Args",
            "type": "array"
          },
          "env": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Environment variables for the spawned stdio command. A value of $NAME is read from the server's own environment at startup, and any secret-bearing key (token, api_key, authorization, ...) must use that form.",
            "title": "Env",
            "type": "object"
          },
          "url": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The endpoint a streamable_http server is reached at. Required for that transport, and rejected for stdio.",
            "title": "Url"
          },
          "headers": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Headers sent with every streamable_http request. A value of $NAME is read from the server's own environment at startup, and any secret-bearing key must use that form.",
            "title": "Headers",
            "type": "object"
          },
          "egress": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Whether this server sends session data off the local network. Tool arguments carry conversation-derived data and neither transport can tell on its own, since a stdio command may proxy anywhere and a url may name localhost, so under server.local_only every referenced entry must declare it.",
            "title": "Egress"
          },
          "tool_timeout_s": {
            "default": 15.0,
            "description": "How long one tool call on this server may take, in seconds, before the model is told it timed out. The device hears silence meanwhile, so keep it short.",
            "exclusiveMinimum": 0,
            "title": "Tool Timeout S",
            "type": "number"
          },
          "instructions": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Guidance for the model about using this server's tools, injected into the system prompt of every agent this entry is granted to, under a heading naming the prefix its tools carry. It is written for the whole entry rather than per tool, so guidance about a tool an agent's allow list withholds is noise the operator avoids by writing about the granted surface. The grant is the whole condition: it is injected whether or not the server is connected and whatever its tools turn out to be, and an agent with `mcp: []` never sees it. It is stored and injected as written, its indentation and its own blank lines included; the only bytes trimmed are whitespace at the two ends of the whole assembled prompt, which is also what the inspection surface reports. Editing it does not restart the connection, so a reload reports the entry as `unchanged`, and the new text reaches a conversation at its next activation: a new session or an agent switch, never a reply of a session already running.",
            "title": "Instructions"
          }
        },
        "required": [
          "transport"
        ],
        "title": "McpServerConfig",
        "type": "object"
      },
      "FillerConfig": {
        "additionalProperties": false,
        "description": "Masking reply latency with a pre-synthesized filled pause.\n\nOff by default. When enabled, the phrases are synthesized in the\nagent's own voice at boot and cached as PCM; a reply whose first\naudio has not started within `delay_ms` of the utterance being\ntranscribed plays one, and the real reply queues behind its tail.\nA synthesis failure at boot logs a warning and leaves the feature\noff for that agent rather than failing the boot.",
        "properties": {
          "enabled": {
            "default": false,
            "description": "Whether a filled pause is played while a slow reply is prepared. Off by default.",
            "title": "Enabled",
            "type": "boolean"
          },
          "delay_ms": {
            "default": 1800.0,
            "description": "How long the user hears silence before the filler starts, in milliseconds, counted from the transcription of their utterance.",
            "exclusiveMinimum": 0,
            "title": "Delay Ms",
            "type": "number"
          },
          "phrases": {
            "description": "The phrases to play, written in the agent's own language; the player rotates through them rather than always playing the same one. At least one is required when the feature is enabled.",
            "items": {
              "minLength": 1,
              "type": "string"
            },
            "title": "Phrases",
            "type": "array"
          }
        },
        "title": "FillerConfig",
        "type": "object"
      },
      "McpGrant": {
        "additionalProperties": false,
        "description": "One `mcp` list entry in its object form: a server, and which of\nits tools the layer may reach.\n\nA second spelling of the same list entry rather than a different\nthing. A plain string names the whole server, and this names part of\none, so an agent that should switch the lights but not unlock the\ndoor does not need a second server to say it in.\n\nTools are named by the published name without its entry prefix\n(`turn_on_light` grants `home__turn_on_light`), matched exactly. That\nidentifier is this application's own: it has been through the\npublishing rule, it is what `samtal-server config status` shows and\nwhat the model calls, so an operator writes down the name they read.\nWhat a server listed before the rule got to it never appears on a\nsamtal surface, and cannot be granted by.",
        "properties": {
          "server": {
            "description": "The MCP server this grant is about, by the name it is defined under in mcp_servers.",
            "minLength": 1,
            "title": "Server",
            "type": "string"
          },
          "tools": {
            "anyOf": [
              {
                "items": {
                  "minLength": 1,
                  "type": "string"
                },
                "minItems": 1,
                "type": "array",
                "uniqueItems": true
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Which of that server's tools this layer may reach, by the published name without its entry prefix (turn_on_light for home__turn_on_light), which is the name `samtal-server config status` shows. Leaving it out grants the whole server, exactly as naming the server as a plain string does. A name that matches nothing the server published is not an error at write time, since only a live connection knows the list; it is logged when the server publishes and visible under grants on the status surface.",
            "title": "Tools"
          }
        },
        "required": [
          "server"
        ],
        "title": "McpGrant",
        "type": "object"
      },
      "AgentConfig": {
        "additionalProperties": false,
        "description": "One agent: a prompt, plus whichever stages it overrides.",
        "properties": {
          "llm": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The language model, by the name it is defined under in providers.llm. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Llm"
          },
          "asr": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The speech recognizer, by the name it is defined under in providers.asr. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Asr"
          },
          "tts": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The voice, by the name it is defined under in providers.tts. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Tts"
          },
          "vad": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The voice activity detector, by the name it is defined under in providers.vad. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Vad"
          },
          "mcp": {
            "anyOf": [
              {
                "items": {
                  "anyOf": [
                    {
                      "minLength": 1,
                      "type": "string"
                    },
                    {
                      "$ref": "#/components/schemas/McpGrant"
                    }
                  ]
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The MCP servers whose tools this layer offers the model. An entry is either the entry name on its own, which is the whole server, or an object naming the server and the tools of it this layer may reach ({server: home, tools: [turn_on_light]}), where a tool is named by its published name without the entry prefix. Unset inherits the agent_defaults list; naming a list replaces the inherited one rather than extending it, so an empty list opts an agent out of the tools its siblings have. The builtin tools are outside this model: switch_agent and remember appear under a structural condition (a device bound to more than one agent, memory configured) rather than by grant.",
            "title": "Mcp"
          },
          "filler": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FillerConfig"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Latency masking with a pre-synthesized filled pause. Unset inherits the agent_defaults section; naming one replaces it wholly rather than merging with it, so `filler: {enabled: false}` opts an agent out."
          },
          "prompt": {
            "default": "",
            "description": "The instruction this agent replies under, sent as the system prompt on every turn. State the reply language explicitly: a model otherwise picks one by its training bias.",
            "title": "Prompt",
            "type": "string"
          }
        },
        "title": "AgentConfig",
        "type": "object"
      },
      "AgentDefaults": {
        "additionalProperties": false,
        "description": "Provider references every agent inherits unless it names its own.\n\nDeliberately no prompt: an agent's prompt is its identity, and\ninheriting one silently would make two agents the same agent.",
        "properties": {
          "llm": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The language model, by the name it is defined under in providers.llm. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Llm"
          },
          "asr": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The speech recognizer, by the name it is defined under in providers.asr. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Asr"
          },
          "tts": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The voice, by the name it is defined under in providers.tts. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Tts"
          },
          "vad": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The voice activity detector, by the name it is defined under in providers.vad. An agent that leaves it unset inherits the agent_defaults entry.",
            "title": "Vad"
          },
          "mcp": {
            "anyOf": [
              {
                "items": {
                  "anyOf": [
                    {
                      "minLength": 1,
                      "type": "string"
                    },
                    {
                      "$ref": "#/components/schemas/McpGrant"
                    }
                  ]
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The MCP servers whose tools this layer offers the model. An entry is either the entry name on its own, which is the whole server, or an object naming the server and the tools of it this layer may reach ({server: home, tools: [turn_on_light]}), where a tool is named by its published name without the entry prefix. Unset inherits the agent_defaults list; naming a list replaces the inherited one rather than extending it, so an empty list opts an agent out of the tools its siblings have. The builtin tools are outside this model: switch_agent and remember appear under a structural condition (a device bound to more than one agent, memory configured) rather than by grant.",
            "title": "Mcp"
          },
          "filler": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FillerConfig"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Latency masking with a pre-synthesized filled pause. Unset inherits the agent_defaults section; naming one replaces it wholly rather than merging with it, so `filler: {enabled: false}` opts an agent out."
          }
        },
        "title": "AgentDefaults",
        "type": "object"
      },
      "DeviceBinding": {
        "additionalProperties": false,
        "description": "What a device write carries: the agents the device may reach.",
        "properties": {
          "agents": {
            "description": "The agents this device is bound to, by name. The first is the agent a conversation starts on and the rest are the ones switch_agent can reach. Every name has to be an agent that exists, or the write is refused.",
            "items": {
              "type": "string"
            },
            "title": "Agents",
            "type": "array"
          }
        },
        "required": [
          "agents"
        ],
        "title": "DeviceBinding",
        "type": "object"
      },
      "DefaultAgentName": {
        "additionalProperties": false,
        "description": "What a default-agent write carries. Clearing it is the DELETE,\nnot a null here: one way to say a thing.",
        "properties": {
          "name": {
            "description": "The agent an unbound device reaches. It has to be an agent that exists. To unset it, DELETE this resource, which leaves the devices map as the allowlist.",
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "DefaultAgentName",
        "type": "object"
      },
      "SecretValue": {
        "additionalProperties": false,
        "description": "What a secret write carries: the credential itself, the only\nplaintext this API ever accepts.",
        "properties": {
          "secret": {
            "description": "The credential, in plaintext, stored encrypted under the newest key in SAMTAL_MASTER_KEY. It crosses the connection as itself, which is why the whole API belongs on a loopback connection or behind TLS. It is never read back: a read names the slot and masks the value. It may not be empty.",
            "minLength": 1,
            "title": "Secret",
            "type": "string"
          }
        },
        "required": [
          "secret"
        ],
        "title": "SecretValue",
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearerToken": {
        "type": "http",
        "scheme": "bearer",
        "description": "The value of the environment variable server.api.secret_env names. It grants everything this API can do, secret writes included, so it belongs on a loopback connection or behind TLS."
      }
    }
  },
  "security": [
    {
      "bearerToken": []
    }
  ]
}
