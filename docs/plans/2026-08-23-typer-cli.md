# Rebuild the config CLI on Typer: inline values, apply, export

Issue #194, first substantive issue of the #265 CLI chain.
Deviations, resolutions and discoveries land in the companion
`2026-08-23-typer-cli-implementation.md`, one section per milestone,
appended in the change that ticks the milestone.

## Goal

Replace the config CLI's ~800 lines of argparse machinery with a
Typer argument layer, add inline `key=value` writes beside `-f`,
add `config apply` (one document, dependency-ordered, refused
whole) and `config export` (the apply-able round trip), and give
the CLI its documentation home in `docs/reference/cli.md` with a
generated command reference under the existing drift discipline.
The valuable halves of `cli.py` (rendering, refusal sentences,
transport policy, the acts table) carry over; the parser subclass,
the hand-rolled subcommand tree and the manual dispatch wiring go.

## The issue's decisions, restated

- Typer for the argument layer; rendering, refusals and safety
  checks move unchanged; expected saving is a few hundred lines and
  lower friction per new subcommand.
- `set` accepts `key=value` pairs assembling the same fragment the
  YAML would, validated by the same models on the same write path;
  dotted keys nest, values parse as YAML scalars; `-f` stays, the
  two forms are alternatives for one command.
- `config apply -f setup.yaml`: one document, any number of
  entities, applied through the existing write path in dependency
  order, idempotent, refused whole on an unresolvable reference
  with the same sentences a single write earns.
- `config export`: the current store as an apply-able document, and
  per-entity; export is the writable projection, `show` the display
  one, per #192's read-modify-write contract; secrets never travel
  (env name or stored marker), `set-secret` stays the only door.
- `docs/reference/cli.md`: hand-written head (installation, remote
  use, `--local` break-glass), generated command reference under
  the drift checks.
- Pre-release: nothing owed to the current grammar; what survives
  does so on merit. The docgen no-database property survives.
  `reload` stays #191's verb, `diff` stays #193's reserved seat.
  OpenAPI-generated clients stay out of scope.

## What changed under the issue since it was written

The issue predates the vinga rename, #242/#243, and #244. The
re-census at `df758e8e`: `cli.py` is 2,035 lines; the doctor left
it in #244 (so "the doctor probes carry over" is satisfied
elsewhere and this plan does not touch `doctor.py`); reads derive
from the descriptor registry (#207) and the display envelope IS
the writable projection (#192's recorded resolution: "the marker
makes the display envelope the writable projection, which is the
resolution #194's per-entity export inherits"); the store refuses
unresolved references at every write (`_refuse_unresolved`); and
`config schema`/`reference`/`openapi` carry child-interpreter
import pins.

## Decisions

### 1. The rebuild happens in place; #223 packages it afterward

Two ratified texts pull apart. The 2026-08-20 comment on this
issue says the Typer layer "should be built in that standalone
package directly rather than inside the server and moved later".
The #265 tracking issue, written 2026-08-23 and the operative
sequencing, says the opposite order: "#223 (standalone CLI) after
#194. It packages the rebuilt CLI, not the old one", with #223
still carrying the layout decisions (in-repo workspace member
versus separate repo, generated versus drift-checked shapes).

This plan follows #265, for three reasons. It is the newer
decision. Building in a standalone package now would need #223's
undecided layout first, inverting the ratified order. And the
grammar cannot move wholesale anyway: `--local` and the four
break-glass commands stay in the server package by #223's own
terms, so a package-first build would split one grammar across two
packages in the same change that rebuilds it. What this plan owes
the 2026-08-20 comment instead is extraction cleanliness: the
Typer app and the acts keep their strict client shape (HTTP,
rendering, no store import outside the `--local` arms), so #223's
move is a relocation, not an untangling. Recorded here so the plan
review and Rafael can veto the reading.

### 2. Typer joins the runtime dependencies; the boundary is ours

`typer` becomes a direct dependency (click is already in the tree
transitively via uvicorn). The app runs with
`standalone_mode=False` always: Click's own error path prints and
exits 2, and this CLI's contract is a ConfigError sentence on
stderr and exit 1, with `--help` leaving through exit 0.

The usage-error boundary adopts the stricter sibling shape
(`conversations/cli.py`'s marker table plus a deliberately vague
fallback) rather than porting `_usage_problem`'s narrow one. That
closes a pre-existing hole the census surfaced: today's config
grammar passes argparse's `invalid choice: 'x'` through verbatim,
echoing the typed word, while every sibling grammar refuses to.
Click's `UsageError` subclasses (NoSuchOption with its
did-you-mean, BadParameter quoting the value) are translated by
CLASS to fixed sentences, never by their text. The
secret-never-an-argument sentence stays on the
unrecognized-arguments shape. This is a recorded strengthening in
the changelog, and the grammar suite grows a planted-secret case
per usage-error shape.

Two argparse behaviors are load-bearing pins and must survive the
library change exactly:

- `--config`, `--api-url` and `--local` are accepted both before
  and after the subcommand, and a pre-command value is not
  clobbered by the command's absent one (today's
  `default=argparse.SUPPRESS` dance). Shape: the Typer root
  callback holds the three options; each command declares its own
  copies defaulting to an UNSET sentinel; resolution merges
  per-option, command-position winning only when given. The
  existing coverage is thinner than the plan first claimed (only
  `--local` is exercised after a leaf command; nothing proves a
  command-position value beats a root-position one), so M1 adds
  the matrix, stated separately for the two positions the grammar
  really has: at the ROOT all three options are accepted before
  every command; at the LEAF the exclusions bind (the three
  offline documentation commands take no leaf options, `ota-url`
  takes `--config` only). Value conflicts are tested for
  `--config` and `--api-url` (command position wins); `--local`
  is presence-only, so its cases are duplicate presence and a
  root `True` surviving an absent leaf copy. All parameterized so
  a fourth global option inherits the matrix.
- `cli.main(argv: Sequence[str] | None) -> int` and
  `cli.build_client` keep their exact signatures and module
  positions: `main.py`'s word dispatch and every acceptance
  suite's monkeypatch depend on them, and neither changes.

`conversations/cli.py`, `events_cli.py`, `doctor.py` and
`main.py`'s own parser stay argparse: they are three small stable
grammars, and this issue buys Typer where the entity count grows,
not everywhere. The repository carrying two argument libraries is
recorded as the accepted cost.

### 3. The grammar keeps every spelling a committed artifact pins

Four command spellings are baked into shipped event messages and
the committed `events.md`: `config reload`, `config add-device`,
`config bind-device`, `config ota-url`. Two more live in write
notices and OpenAPI bytes (`config set`, `config set-secret`,
`config show device`). The redesign freedom is real but these
spellings stay; the pre-release license is for shapes that pin
nothing, and none of these earns a rename worth moving three
artifacts for. The grammar therefore keeps its verb set and
positional shapes; what changes is the machinery underneath, the
usage sentences (decision 2), and the additions (`key=value`,
`apply`, `export`).

### 4. Inline values assemble the fragment, and nothing else

`set <kind> [identity] key=value...`: each pair splits on the
first `=`, the key splits on dots into nesting, the value parses
through `yaml.safe_load` of the value string alone and is then
held to SCALAR-ONLY: a value that parses to a list or a mapping
is refused, because "values parse as YAML scalars" is the
contract and a nested structure belongs in a fragment. The
assembled mapping enters the exact path a `-f` fragment enters:
`check_transportable`, the same PUT, the repository's validation,
the same acknowledgement. `-f` and pairs are exclusive; neither
given is the missing-argument usage error.

The pair parser is a no-leak boundary of its own, built the way
`_fragment` is: every refusal is a fixed, value-free sentence
(a pair with no `=`, an empty key or an empty dotted segment, an
exact duplicate key, a prefix collision like `a.b=1 a=2`, a value
that will not parse, a non-scalar value), the YAML parser's
exception is caught without retaining its input (record the
category, raise after the arm, no `__cause__`/`__context__`), and
the grammar suite plants a credential-shaped value against every
one of those shapes asserting absence from stdout, stderr, log
records and exception chains. The help text for `set` carries the
warning that a secret is never a `key=value` argument (the store
already refuses plaintext secrets by key shape; what the help
adds is the shell-history reason).

Help earns the "generated from the descriptors" claim: every
command's help shows its options with defaults and descriptions
(Typer's native rendering covers the options), and every entity
`set` help shows each model field's TYPE, DEFAULT and description,
which means `docgen.fragment_help` deepens to render the type and
default columns `docgen.type_name` and `docgen.default` already
compute for the reference. A test enumerates the Typer command
tree and holds each `set` help to the model it documents, so a
field added to a model appears in the help without anyone
remembering to.

### 5. `local_ok` becomes declarative, closing the recorded hole

Today a command joins the break-glass subset by an imperative
`set_defaults(local_ok=True)`, and
`test_config_cli_local.py:249-253` records that its differential
MUTATIONS list is held complete by review alone. In the Typer
layer, break-glass membership is a fact on the command
registration (one table of command factories carrying
`local_ok`), and a new test derives the expected MUTATIONS
coverage from that table, so a future `local_ok=True` command
that skips the differential fails a test instead of a review. The
differential acknowledgement pin itself
(`test_a_local_write_acknowledges_what_the_api_acknowledges`) and
its wording sibling carry over unchanged in meaning.

### 6. `apply` is a repository verb served by the API

The refused-whole bound decides the architecture: a CLI-side loop
of PUTs cannot refuse a document whole, because the store
validates references per write inside per-write transactions, and
stopping at the first refusal leaves the store half-applied. So
apply lands where the transaction is:

- `ConfigStore.apply(document)` cannot be a loop over the existing
  write internals: `_write` opens its own transaction and
  `_persist` runs `_refuse_unresolved` after each entity, so
  composing them either nests transactions or rejects intermediate
  states a complete document would resolve. The write path
  therefore refactors into transaction-neutral phases that single
  writes and apply share: parse and per-kind checks outside the
  lock where possible; one transaction; markers resolved against
  one snapshot; every incoming entity staged into one candidate
  domain state; `check_references` run ONCE against that final
  state; then all rows persisted. A single `set` becomes the
  one-entity case of the same phases, so there is one write path,
  not two. Refusals aggregate into one ConfigError whose lines are
  the same sentences the single writes earn; any refusal rolls
  back everything.
- The store and the route split the way single writes already
  split: `ConfigStore.apply` returns canonical outcomes only
  (`wrote` or `unchanged` per entry), and `POST /apply` (RawBody,
  unread by FastAPI, like every write) computes what the store
  cannot know: the per-entry notices using the loaded-agents and
  snapshot-only dependencies the settings routes already use,
  pending-code retirement for every device MAC a successful
  commit bound, and the retire-all-pending rule when a non-null
  default agent was explicitly applied. All of that housekeeping
  runs only after the transaction commits, and the tests cover
  both changed and unchanged rows. The answer lists the per-entity
  outcomes and notices. The OpenAPI document moves, deliberately,
  regenerated under its byte pin, and the admin UI (#129) inherits
  a batch verb for free.
- `vinga-server config apply -f setup.yaml` posts it. Apply is not
  in the `--local` recovery subset (the subset stays the four
  commands; recovery does not need batches).

The document is a PARTIAL DomainConfig-shaped document: top-level
keys are the `DOMAIN_KEYS` sections, entity bodies exactly the
fragments `set` takes, and the two settings in their DOMAIN shape,
not their route-body shape: `devices` as `mac: [agents]` (the
route's `{agents: [...]}` is an adapter concern) and
`default_agent` as a scalar or an explicit null. Apply adapts
each settings entry onto the existing bind-device,
set-default-agent and clear-default-agent write semantics.

Omission is additive, stated as the rule: a section or entry the
document does not mention is untouched; an empty mapping adds
nothing; an explicit `default_agent: null` clears the setting,
and its absence leaves it alone. Apply never deletes: pruning a
store down to a document is a different verb with different
stakes (secret deletion among them), deliberately not built here,
and `cli.md` says so beside apply.

Idempotence is comparison, not blind rewrite: for each entity the
store compares the incoming body against the stored one through
the same write-shaped projection reads use (masks resolving to
keep-stored per #192's marker), and an equal body is reported
`unchanged` with no write and no notice. The same file twice is
therefore a no-op by construction.

The dependency order needs no new structure: `DOMAIN_KEYS` in
`models.py` is already documented as the write, read and creation
order, and apply iterates it directly, so there is no second list
to drift (the plan's earlier `APPLY_ORDER` idea is withdrawn; a
duplicate of an authoritative order is the design guide's
textbook violation, and cross-kind behavior does not belong in
`entities.py`, whose contract excludes behavior). Two honest
guards, because a planted-edge apply test could not fail (apply
validates once against the final candidate, so any complete
document succeeds in any iteration order): creation order is
exercised through sequential SINGLE writes per reference edge,
where order is observable and the wrong one is refused; and
apply's outcome listing is asserted to be emitted in
`DOMAIN_KEYS` order, the only order apply itself observably has.

### 7. `export` is a CLI-side reshape of the reads that already exist

`config export` fetches the whole document read and the per-entity
envelopes the API already serves and emits the apply-shaped
document; `config export <kind> [identity]` emits one entity's
fragment. No new read endpoint: #207 made every read derive from
the descriptor registry and stay write-shaped, and #192's marker
made the display envelope the writable projection, so export is
assembly, not translation.

Secrets: the exported entity BODIES are exactly the read
envelopes' bodies, unchanged. An env reference is a body value
and travels as itself. A STORED secret is not in the body at all
(the envelope reports it separately, and the CLI has always
rendered it as a comment, because the mask is not a creatable
value); export keeps that: stored slots become YAML comment
annotations naming the slot and `set-secret`, never mask values
injected into bodies, since a mask in a CREATING write is
refused and would make apply-to-an-empty-store fail on its own
export. The supported reproduction sequence, documented in
`cli.md` and in the export header comment, is: apply the exported
entities, then run `set-secret` for each annotated slot. The
round-trip suite extends along exactly that order: seed a store
with env-referenced, stored-only and shadowing secrets; export;
apply onto a fresh store; run the annotated `set-secret`s; export
again; the two exports byte-identical.

### 8. `docs/reference/cli.md`, hand head and generated body

The hand-written head covers installation (docker exec shim,
`uvx`, `uv tool install`), remote use (`--api-url`/`VINGA_API_URL`
resolution order, the token variable, loopback-or-TLS), and
`--local` as break-glass. The generated half renders the command
reference from the Typer app: a new `config cli-reference` verb
prints it, the committed copy is byte-diffed by a docgen-suite
test and a CI step beside the four existing lanes, and the READMEs
link to the page instead of carrying partial copies.

The drift protocol for a half-generated page, stated so it is
implementable: the generated region sits between two stable
marker comments (`<!-- generated: cli reference -->` and its
closing pair); the renderer emits deterministic text (no color,
fixed width, no terminal detection; Click's help formatter is
driven with an explicit fixed-width context); the drift test and
the CI step extract the region between the markers from the
committed page and byte-compare it against a fresh render,
leaving the hand-written head untouched. The renderer lives in
`cli.py` beside the app it renders: its only caller is the
`cli-reference` command, and a module for one caller would be the
pass-through the design guide deletes.

Import weight: the generator imports the app module, which imports
the client machinery, so it is the second deliberate exception
beside `docgen.openapi()`. The child-interpreter pin
(`ALLOWED_IMPORTS` exact set) covers `schema` and `reference`
only and does not change; the no-database property is pinned for
the new verb the way it is for the other three (the `/nowhere`
database dir fixture), since rendering help opens nothing.

### 9. Presets and the documentation sweep

Four deliverables, the last three being #265's own lower-half
assignments to this issue:

- **Presets.** `examples/presets/local-stack.yaml`
  (faster-whisper ASR, an openai-compatible local LLM, piper TTS,
  silero VAD, one agent, defaults, a device-less bootstrap) and
  `examples/presets/cloud-stack.yaml` (the cloud trio), each a
  complete apply document with secrets as env references. The
  examples-ownership docgen test learns the presets tier.
- **Derived recipes.** Per-topic CLI command sequences (providers,
  agents, MCP servers, devices, fillers, secrets) RENDERED by
  docgen from the same descriptor-owned `examples/*.yaml`
  fragments, never hand-written beside them: two structures that
  must agree are one structure. The recipe renderer gets its own
  drift lane like the other generated documents, and every
  published recipe runs against the M3 live lane in CI, so a
  recipe that stopped working stops merging.
- **The fileless quick start.** The chain verifies the server
  boots with NO config file when `VINGA_SERVER__*` variables
  suffice (an integration case in the M3 lane boots uvicorn from
  environment alone), fixing whatever that surfaces, and the
  quick start rewrites to `docker run` with a handful of env vars
  plus the recipe commands; the YAML file stays the declarative
  option, never the requirement.
- **The sweep.** The repository README's six heredocs collapse to
  one `vinga config apply` line against a preset; the
  vinga-server README's bootstrap block likewise; the manual
  order paragraphs shrink to a sentence saying apply orders for
  you; `config.deploy.example.sh` migrates onto a preset apply or
  is retired, with the choice and reasons recorded in the
  implementation doc; `config.example.yaml`'s comment block
  updates with the rest. Every remaining `config ...` spelling
  site the census lists is swept in the same milestone as the
  grammar piece that changes it.

### 10. Milestones recut from #265's sketch, for releasability

#265 sketched "the Typer skeleton with read commands, then the
write and secret commands, then declarative apply". Cut that way,
M1 would ship a config CLI whose writes still parse under argparse
beside a Typer half, two co-equal grammars for one command group,
which is the shape the pipeline's own rule forbids merging. The
recut keeps #265's substance, including the deliverables its lower
half assigns to this issue, in four releasable milestones:

- M1 moves the WHOLE grammar to Typer mechanically: every command,
  current shapes, `-f` writes intact, the decision-2 boundary and
  strengthened usage sentences, `local_ok` declarative (decision
  5), suites migrated. Behavior deltas: usage sentences only.
- M2 adds inline `key=value` (decision 4) and the apply/export
  pair (decisions 6 and 7): the phased write path, the route, the
  two commands, the round-trip extension. The OpenAPI artifact
  moves here.
- M3 is the live acceptance lane #265 assigns to this issue: an
  integration suite booting real uvicorn (the `served_api`
  fixture, the `test_ota_endpoint.py` pattern), running
  `cli.main` with an UNPATCHED `build_client` over the wire.
  Coverage is derived from the registered command inventory (the
  declarative table decision 5 creates), driving every command
  family with structure-and-semantics assertions and a
  representative refusal per family; a completeness test holds
  the lane's coverage list to the registration table so a new
  command cannot skip the lane silently. Wheel-and-subprocess
  grade stays #223's.
- M4 is the documentation milestone: `cli.md` with its generated
  half and drift lane (decision 8), the presets, the derived
  recipes, the fileless quick start, and the README and examples
  sweep (decision 9).

## Module layout after the change

- `config/cli.py`: the Typer app, the acts, the boundary; loses
  `_Parser`, `_usage_problem`'s narrow shape, `_parser()`'s ~300
  lines of tree building; keeps rendering, transport, `_call`,
  `build_client`, `main`.
- `config/entities.py`: unchanged in shape; apply iterates
  `models.DOMAIN_KEYS`, which already states the order.
- `config/store.py`: gains `apply(document)` over the existing
  write internals.
- `config/api.py` and `config/responses.py`: the `/apply` route
  and its response model; regenerated OpenAPI.
- `config/cli.py`: also the cli-reference renderer, beside its
  one caller (decision 8).
- `docs/reference/cli.md`: new, committed, drift-checked.
- `examples/presets/`: new tier, two files.
- `pyproject.toml` and `uv.lock`: the `typer` runtime dependency,
  both in M1, because CI installs `--frozen`.
- `config.example.yaml`: its command-guidance comment block
  updates in M4's sweep (the server SCHEMA does not change, which
  is what the keep-in-sync rule is about; the comments are
  documentation and move with the docs).
- `CHANGELOG.md`: Changed (grammar machinery, usage sentences
  strengthened), Added (inline values, apply, export, cli.md,
  presets).

## Tests

M1 is pinned by the existing eight CLI suites: assertion content
survives except where a usage sentence deliberately changed
(decision 2's recorded list); the grammar suite grows the
per-shape planted-secret cases; the local suite's differential
pins carry; the SUPPRESS-equivalent both-positions behavior is
already exercised by the `--local`-first cases and must stay
green. M2: inline-value cases beside each fragment case (same
resulting store state, proven by the round-trip read), apply
cases (order, idempotence, refused-whole with rollback proven by
reading the store after, aggregation wording equal to the single
writes' sentences), export round trip per decision 7. M3: the
live lane per decision 10: inventory-derived coverage held
complete against the registration table, a representative refusal
per family over the wire, the fileless-boot case, and the two
apply-bound real-socket proofs. M4: the marker-region drift lane
for `cli.md`, the recipe lane (drift plus a live-lane run per
recipe), the presets applying green onto a fresh store, and the
docgen ownership tier. Inventories by tooling: the census's
spelling-site list is the sweep's checklist, re-grepped at review
time.

## Risks

- **Click's error surface is wider than argparse's.** Unknown
  options carry did-you-mean suggestions, bad values quote
  themselves; any shape not translated by class leaks. Mitigation:
  the boundary translates by exception CLASS with a vague
  fallback, and the grammar suite plants a secret in every shape
  the boundary names.
- **The both-positions option merge is subtle.** The sentinel
  merge must reproduce SUPPRESS semantics exactly; the local
  suite's `--local`-first cases are the pin, and M1 does not merge
  until they pass unmodified.
- **`store.apply` under concurrent writers, and the bounded
  batch.** It rides the same single-transaction discipline every
  write rides (the busy timeout and the retryable 409). The 30 s
  read timeout bounds the lock WAIT, not the transaction an
  arbitrarily large document holds after acquiring it, and a
  client that gives up on a write the server later commits is the
  exact unknown-outcome the timeout rationale exists to avoid. And
  no finite client timeout can be derived, because apply loads the
  whole existing store and validates the whole resulting domain,
  whose size no request bound limits. So apply's client read
  timeout is NONE: the CLI waits for the server's answer however
  long the transaction takes, the `RELOAD_READ_TIMEOUT_S`
  rationale taken to its conclusion where no finite envelope
  exists. The entity-count limit on the document (refused over it
  before any mutation, fixed sentence naming the limit) and the
  request body size bound stay as request hygiene; the connect
  timeout stays bounded. What remains is transport death
  mid-wait, the exposure every write already has, recovered the
  same way (read the store back with `export` or `show`), and
  `cli.md` says so beside apply. The real-socket lane proves a
  large accepted batch completes with the client waiting it out,
  and an over-limit one is refused unmutated.
- **Artifact churn.** Only the OpenAPI document and
  `domain-config.md` may move (M2's route; any registry `command`
  text if a spelling changes, which decision 3 forbids), plus the
  new `cli.md`; `events.md` must not move, and that is asserted at
  each milestone's drift run.

## Milestones

- [x] **[M1: the grammar moves to Typer whole.](2026-08-23-typer-cli-implementation.md#m1-the-grammar-moves-to-typer-whole)**
  (PR #271)
  Decision 2 and 5's shapes, decision 3's spelling constraint,
  every command on the new layer with current argument shapes and
  `-f` writes; the two seams (`main`, `build_client`) unchanged;
  `pyproject.toml` plus `uv.lock` carry the new dependency here.
  Design footprint: deepens `cli.py` (the grammar becomes one
  declarative table the way the acts already are; callers of the
  module notice nothing); no new modules.
- [x] **[M2: inline values, apply, export.](2026-08-23-typer-cli-implementation.md#m2-inline-values-apply-export)**
  (PR TBD)
  Decisions 4, 6, 7: the pair grammar, the phased write path,
  `ConfigStore.apply` iterating `DOMAIN_KEYS`, `POST /apply`, the
  two commands, the round-trip extension; OpenAPI regenerated.
  Design footprint: deepens `store.py` (single writes become the
  one-entity case of the shared phases, no second write path);
  adds one route.
- [ ] **M3: the live acceptance lane.**
  The uvicorn-over-the-wire suite per decision 10: unpatched
  `build_client`, inventory-derived coverage held complete by a
  test against the registration table, a representative refusal
  per family. Design footprint: test assets only; the one
  production-adjacent piece is whatever small hook the inventory
  derivation needs, and it must be a read of the registration
  table, not a parallel list.
- [ ] **M4: cli.md, recipes, presets, the sweep.**
  Decisions 8 and 9 as amended: the reference page and its drift
  lane, the recipe renderer with its own drift-and-run lane, the
  two presets, the fileless quick start verified and rewritten,
  `config.deploy.example.sh` migrated or retired with reasons,
  `config.example.yaml`'s comment block updated, and the README
  and examples collapse onto `apply`. Design footprint:
  documentation and two renderers; no production seams.

## Plan review round

External review of commit `e1661cb9`, 2026-08-23. Backend: codex
CLI 0.149.0, model `gpt-5.6-sol`, read-only sandbox, runtime
6m11s. Verdict as received: ready after the P1/P2 amendments.
Thirteen findings, seven P1; each amendment is its own commit with
a resolution note here. Findings 7 and 8 corrected the plan
author's own reading: #265's body carries, below its estimates
table, the live-uvicorn acceptance lane, the derived recipes and
the fileless quick start as work landing with this issue, and the
plan had been written from a truncated read.

1. **P1: the proposed store composition cannot provide one
   transaction and final-state validation.** `_write` opens its
   own transaction and `_persist` runs `_refuse_unresolved` after
   each entity, so calling the internals in order either nests
   transactions or rejects intermediate states.

   *Resolution* (this commit): decision 6 now requires the
   two-phase refactor: transaction-neutral preparation and
   persistence shared by single writes and apply, one transaction,
   markers against one snapshot, one candidate domain state, one
   `check_references` pass, then persistence; a single `set` is
   the one-entity case of the same phases.

2. **P1: the apply document contradicted the existing shapes for
   devices and the default agent.** Both are settings, not
   fragment entities: their write bodies are `{agents: [...]}` and
   `{name: ...}`, while the domain document holds `mac: [agents]`
   and a scalar-or-null; omission semantics were unstated.

   *Resolution* (this commit): the document is a partial
   DomainConfig-shaped one with the settings in their domain
   shapes and adapters onto the existing settings write semantics;
   omission is additive, an explicit `default_agent: null` clears,
   and apply never deletes, stated in `cli.md` beside the verb.

3. **P1: export described stored secrets incorrectly and its
   fresh-store test skipped the real bootstrap problem.** A stored
   ciphertext is not a mask in the writable body; it is reported
   separately and rendered as a comment because the mask is not a
   creatable value, and apply-to-empty would fail on it while
   `set-secret` cannot run before the entity exists.

   *Resolution* (this commit): decision 7 keeps exported bodies
   identical to the read envelopes, renders stored slots as YAML
   comment annotations naming `set-secret`, and the documented
   reproduction order is apply-then-set-secret; the round-trip
   test follows that supported order over env-referenced,
   stored-only and shadowing secrets with no back-door seeding.

4. **P1: apply omitted API-side device housekeeping and cannot
   produce the promised notices from the store.** Device and
   default-agent notices depend on loaded agents and snapshot-only
   mode, which the store does not know, and the routes retire
   pending codes after a successful commit.

   *Resolution* (this commit): decision 6 splits store and route
   the way single writes split: canonical outcomes from the store;
   notices, pending-code retirement per bound MAC, and the
   retire-all rule for an explicit non-null default agent computed
   by the route, after commit only, tested for changed and
   unchanged rows.

5. **P1: inline scalar parsing lacked the no-leak boundary and
   would accept non-scalars.** `yaml.safe_load` accepts lists and
   mappings, and an unhandled YAMLError can retain or print the
   submitted text.

   *Resolution* (this commit): decision 4 holds values scalar-only
   and makes the pair parser a `_fragment`-style boundary: fixed
   value-free refusals for every malformed shape, parser
   exceptions caught without retention, and a planted-secret case
   per shape over all four surfaces.

6. **P1: the apply timeout argument was false for an unbounded
   batch.** Thirty seconds exceeds the lock wait but not the
   duration of an arbitrarily large transaction, recreating the
   unknown-write outcome the timeout rationale exists to avoid.

   *Resolution* (this commit): the risks section now requires a
   documented entity-count limit refused before mutation, a body
   size bound, a dedicated apply timeout sized above the maximal
   accepted batch, and real-socket proof of both sides of the
   bound.

7. **P1: the live-server acceptance deliverable was absent.**
   #265 requires an integration suite booting real uvicorn and
   driving the rebuilt CLI's whole command inventory over the
   wire, refusal paths included; the plan tested presets only
   through the in-process runner.

   *Resolution* (this commit): a fourth milestone structure with
   M3 as the live lane: `served_api`, unpatched `build_client`,
   `cli.main`, coverage derived from the registration table and
   held complete by a test, a representative refusal per family;
   wheel-and-subprocess grade recorded as #223's.

8. **P2: M3 omitted the derived-recipes and fileless-quick-start
   deliverables.** #265 requires per-topic recipes DERIVED from
   the descriptor-owned example fragments and run against the live
   lane, a no-config-file boot verified, and the quick start
   rewritten; `config.deploy.example.sh` was unaccounted for.

   *Resolution* (this commit): decision 9 now carries all four
   deliverables: presets, docgen-derived recipes with their own
   drift lane and a run in the M3 live lane, the fileless boot
   verified in that lane with the quick start rewritten, and the
   deploy script's disposition recorded.

9. **P2: existing fragment help does not meet the generated-help
   requirement.** `fragment_help` renders names and first
   sentences only, omitting types and defaults `docgen.default()`
   already computes.

   *Resolution* (this commit): decision 4 requires option defaults
   and descriptions on every help page and type/default/
   description per model field on every `set` help, with a
   tree-enumerating test holding help to the models.

10. **P2: `APPLY_ORDER` would duplicate `DOMAIN_KEYS`,** which is
    already documented as the write, read and creation order; a
    second structure is the drift the design guide forbids, and
    behavior does not belong in `entities.py`.

    *Resolution* (this commit): `APPLY_ORDER` withdrawn; apply
    iterates `DOMAIN_KEYS` directly. (The delta round then
    corrected the guard itself; see delta finding 3.)

11. **P2: the claimed global-option coverage does not exist.**
    Only `--local` is tested after a leaf command; no test proves
    a command-position value beats a root-position one.

    *Resolution* (this commit): decision 2 gains the parameterized
    before/after/conflict matrix for all three options with the
    documented exclusions.

12. **P2: the mixed hand-written/generated reference had no
    implementable drift protocol,** and the renderer's owner was
    left as "docgen.py or cli.py".

    *Resolution* (this commit): decision 8 states the protocol:
    marker-delimited generated region, deterministic fixed-width
    rendering, region-only byte comparison in the test and the CI
    step; the renderer lives in `cli.py`, its one caller.

13. **P2: the file inventory contradicted itself and omitted the
    lock.** `config.example.yaml` was listed both changed and
    unchanged, and adding typer moves `uv.lock` because CI
    installs `--frozen`.

    *Resolution* (this commit): the inventory lists
    `config.example.yaml` under M4's sweep and `uv.lock` beside
    `pyproject.toml` in M1; per-milestone artifact ownership is
    stated in the milestone entries.

## Delta re-review

A focused re-review of the amended plan (same backend and model,
runtime 4m30s) checked each resolution against the code and the
plan's own consistency. Six delta findings; verdict as received:
not ready, on the strength of the first. Each amendment is its own
commit below the first round's.

1. **P1: the apply timeout was still not truly bounded.** Apply
   loads the whole existing store and validates the whole
   resulting domain, and the store's size is unbounded, so a
   "maximal accepted batch" has no finite worst case and a derived
   client timeout can still expire after commit.

   *Resolution* (this commit): apply's client read timeout is NONE:
   the CLI waits for the server's answer however long the
   transaction takes, for exactly the reason `RELOAD_READ_TIMEOUT_S`
   gives (a client that gives up on a write the server then commits
   recreates the ambiguity the timeouts exist to avoid), taken to
   its conclusion where no finite envelope exists. The entity-count
   and body-size bounds stay as request hygiene refused before
   mutation. The connect timeout stays bounded. What remains is
   transport death mid-wait, the same exposure every write already
   has, and the recovery is the same: read the store back
   (`export`/`show`); `cli.md` says so beside apply. The
   real-socket case asserts a large accepted batch completes with
   the client waiting it out.

2. **P2: the option matrix contradicted the grammar and held an
   impossible `--local` conflict.** All three globals exist at the
   root, exclusions bind at the leaf, and `--local` is
   presence-only.

   *Resolution* (this commit): decision 2's matrix states root and
   leaf expectations separately; value conflicts only for
   `--config` and `--api-url`; `--local` is tested for duplicate
   presence and for a root `True` surviving an absent leaf copy.

3. **P2: the `DOMAIN_KEYS` edge guard could not detect a wrong
   order.** Apply validates once against the final candidate, so
   any complete document succeeds in any iteration order.

   *Resolution* (this commit): the guard becomes two honest
   assertions: creation order is tested through sequential single
   writes, where reference order is observable and refusals bite;
   and apply's OUTCOME LISTING is asserted to be emitted in
   `DOMAIN_KEYS` order, which is the only order apply itself
   observably has.

4. **P2: the module inventory still said "docgen.py or cli.py".**

   *Resolution* (this commit): the inventory names `config/cli.py`
   alone, matching decision 8.

5. **P2: the Tests section still assigned M4 deliverables to M3.**

   *Resolution* (this commit): the Tests section is rewritten to
   the four-milestone cut: M3 carries the inventory-derived live
   coverage and the fileless-boot case; M4 carries the cli.md
   drift lane, the recipe lane, the presets and the ownership
   tier.

6. **P3: resolution 13 overstated checklist-level ownership.**

   *Resolution* (this commit): `pyproject.toml` plus `uv.lock`
   join M1's checklist entry and `config.example.yaml` joins
   M4's.
