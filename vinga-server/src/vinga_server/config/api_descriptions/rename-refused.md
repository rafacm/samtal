The new name is not one this deployment can address, being empty once the surrounding whitespace is taken off, or holding a slash or a control character; or it is the name the agent already has once both are stripped, which is nothing to rename; or the body is not a JSON object with exactly one key, `to`, holding a string. Nothing sent is quoted back, which on this endpoint covers the new name in every refusal it can meet. Nothing was changed, and none of these clears on its own: each is answered by sending a different name or a different body.
