The stored half was refused: it does not compose into a valid snapshot, or a credential stored in it will not decrypt under the configured keys. The `detail` is fixed and names nothing, which this refusal shares with the reload beside it and with nothing else this API answers with: what was refused is stored state, the sentence for it would be composed over that state, and this read's answers are entity names and labels only. A server started from this store refuses on the same state and names the location it refused on. Nothing was compared here and nothing was changed.
