Either an apply of this server's configuration is already running, or the configuration database's write lock is held by another request, or this server serves a configuration that no store describes and so has nothing to apply. Nothing was changed. The first two clear on their own and the request can be made again; the third is what this server is, and only starting one from a store changes it.
