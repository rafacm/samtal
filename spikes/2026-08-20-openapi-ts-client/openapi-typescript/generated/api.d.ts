/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/config": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Config
         * @description The whole domain configuration, masked, with the location of
         *     every stored secret beside it.
         */
        get: operations["read_config_config_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/providers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Providers
         * @description Every provider, by stage and then by name: a provider is
         *     addressed by the two together, since two stages may hold one
         *     name.
         */
        get: operations["read_providers_providers_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/providers/{stage}/{name}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Provider
         * @description One provider.
         */
        get: operations["read_provider_providers__stage___name__get"];
        /**
         * Write Provider
         * @description Create or replace one provider from a fragment in the shape
         *     the YAML section had.
         */
        put: operations["write_provider_providers__stage___name__put"];
        post?: never;
        /**
         * Remove Provider
         * @description Delete one provider, and the secrets stored on it. Refused
         *     while an agent or the agent defaults still name it.
         */
        delete: operations["remove_provider_providers__stage___name__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/mcp-servers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Mcp Servers
         * @description Every MCP server, by name.
         */
        get: operations["read_mcp_servers_mcp_servers_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/mcp-servers/{name}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Mcp Server
         * @description One MCP server.
         */
        get: operations["read_mcp_server_mcp_servers__name__get"];
        /**
         * Write Mcp Server
         * @description Create or replace one MCP server. The running server applies
         *     it at the next reload, with no restart.
         */
        put: operations["write_mcp_server_mcp_servers__name__put"];
        post?: never;
        /**
         * Remove Mcp Server
         * @description Delete one MCP server, and the secrets stored on it. The
         *     running server stops it at the next reload.
         */
        delete: operations["remove_mcp_server_mcp_servers__name__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/prompt-fragments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Prompt Fragments
         * @description Every shared prompt fragment, by name.
         */
        get: operations["read_prompt_fragments_prompt_fragments_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/prompt-fragments/{name}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Prompt Fragment
         * @description One shared prompt fragment, with its text as it was written:
         *     it is what the model is given, and there is nothing in it to
         *     mask.
         */
        get: operations["read_prompt_fragment_prompt_fragments__name__get"];
        /**
         * Write Prompt Fragment
         * @description Create or replace one shared prompt fragment.
         *
         *     It carries the restart sentence rather than the reload's: what
         *     the reload re-reads is the MCP entries, their secrets and the
         *     grant lists, and a fragment is prompt text the agents compose
         *     with at their next activation on a server that read it at boot.
         */
        put: operations["write_prompt_fragment_prompt_fragments__name__put"];
        post?: never;
        /**
         * Remove Prompt Fragment
         * @description Delete one shared prompt fragment. Refused while any layer
         *     still includes it.
         */
        delete: operations["remove_prompt_fragment_prompt_fragments__name__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/agents": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Agents
         * @description Every agent, by name.
         */
        get: operations["read_agents_agents_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/agents/{name}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Agent
         * @description One agent.
         */
        get: operations["read_agent_agents__name__get"];
        /**
         * Write Agent
         * @description Create or replace one agent. Every provider and MCP server it
         *     names has to exist already, which is what the natural creation
         *     order is about.
         */
        put: operations["write_agent_agents__name__put"];
        post?: never;
        /**
         * Remove Agent
         * @description Delete one agent. Refused while a device binding or the
         *     default agent still names it.
         */
        delete: operations["remove_agent_agents__name__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/agent-defaults": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Agent Defaults
         * @description What every agent uses unless it names something else. One
         *     entry for the whole deployment, and never missing: an unwritten
         *     one reads as the empty entry.
         */
        get: operations["read_agent_defaults_agent_defaults_get"];
        /**
         * Write Agent Defaults
         * @description Replace what every agent uses unless it names something else.
         *     One entry for the whole deployment, so this is a replace and
         *     there is nothing to delete.
         */
        put: operations["write_agent_defaults_agent_defaults_put"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/devices": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Devices
         * @description Every device binding, by the canonical form of its MAC. Bound
         *     devices only: the ones waiting to be claimed are the listing
         *     below, since they have no binding to show.
         */
        get: operations["read_devices_devices_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/devices/pending": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Pending Devices
         * @description Every device showing an activation code, by the code it is
         *     showing.
         *
         *     Runtime state of the server this application is mounted on
         *     rather than stored configuration, so it touches no database and
         *     cannot answer the refusals a read of the configuration can. A
         *     server that restarts forgets it, and the devices come back
         *     within a couple of minutes with fresh codes.
         */
        get: operations["read_pending_devices_devices_pending_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/devices/{mac}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Device
         * @description One device's binding. The MAC is normalized before it is
         *     looked up, so `AA-BB-...` and `aa:bb:...` reach the same
         *     device.
         */
        get: operations["read_device_devices__mac__get"];
        /**
         * Write Device
         * @description Bind one device to one or more agents. The MAC is normalized
         *     before it is written, so the two spellings reach one row.
         *
         *     The running server reads this binding, so the device meets it at
         *     its next check-in rather than at the next restart; what the
         *     acknowledgement says depends on whether the agents named are
         *     ones that server loaded.
         */
        put: operations["write_device_devices__mac__put"];
        post?: never;
        /**
         * Remove Device
         * @description Remove one device's binding, which with no default agent set
         *     means the device is refused at the handshake. Live, with no
         *     agent to be loaded or not: the device stops being served at its
         *     next check-in, though a conversation already running is left to
         *     finish.
         */
        delete: operations["remove_device_devices__mac__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/default-agent": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Default Agent
         * @description The agent an unbound device reaches, or null. Unset is a
         *     configuration rather than a missing entity, so this is never a
         *     404.
         */
        get: operations["read_default_agent_default_agent_get"];
        /**
         * Write Default Agent
         * @description Set the agent an unbound device reaches. Read by the running
         *     server the way a binding is, so it applies to the next device
         *     that asks unless the agent it names was written since boot.
         */
        put: operations["write_default_agent_default_agent_put"];
        post?: never;
        /**
         * Remove Default Agent
         * @description Unset it, leaving the devices map as the allowlist.
         *     Idempotent, like the CLI: there is no such thing as a default
         *     agent that was already not set. Live, like the delete above:
         *     the next unbound device to ask is turned away.
         */
        delete: operations["remove_default_agent_default_agent_delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/providers/{stage}/{name}/secrets/{slot}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Write Provider Secret
         * @description Store one of this provider's credentials, encrypted. The slot
         *     is the option name the credential fills, such as api_key; a
         *     stored secret takes precedence over an environment reference
         *     written for the same slot.
         */
        put: operations["write_provider_secret_providers__stage___name__secrets__slot__put"];
        post?: never;
        /**
         * Remove Provider Secret
         * @description Remove one stored credential. A slot holding none is a 404.
         */
        delete: operations["remove_provider_secret_providers__stage___name__secrets__slot__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/mcp-servers/{name}/secrets/{slot}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Write Mcp Secret
         * @description Store one of this MCP server's credentials, encrypted. The
         *     slot is `env.<KEY>` or `headers.<Key>`, which is where the value
         *     would otherwise have been written as a $VAR reference.
         *
         *     Rotation is exactly what the ciphertext half of the reload's
         *     diff applies, so this carries the reload notice too: the entry
         *     is rebuilt with the fresh credential and reconnected.
         */
        put: operations["write_mcp_secret_mcp_servers__name__secrets__slot__put"];
        post?: never;
        /**
         * Remove Mcp Secret
         * @description Remove one stored credential, which the next reload applies
         *     by rebuilding the entry without it.
         */
        delete: operations["remove_mcp_secret_mcp_servers__name__secrets__slot__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/devices/pending/{code}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Add Device
         * @description Bind the device showing this activation code, and retire the
         *     code.
         *
         *     The MAC comes from the pending entry, so the operator binds what
         *     the board in front of them is showing rather than a MAC they
         *     have to go and find. The write itself is the same repository
         *     call `PUT /devices/{mac}` makes, so reference checking and
         *     transactionality are inherited rather than restated, and the
         *     acknowledgement is the same one, naming the MAC that was bound.
         *
         *     The claim is atomic: the code is reserved before the write and
         *     consumed after it, so two operators racing one code produce one
         *     bind and one retryable refusal. A write that fails releases the
         *     reservation, leaving the code claimable again, because the
         *     device is still showing it.
         */
        post: operations["add_device_devices_pending__code__post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/runtime/mcp-servers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Mcp Server Status
         * @description What each configured MCP server is doing right now.
         *
         *     Runtime state of the server this application is mounted on, like
         *     the pending listing: it touches no database, so it can answer
         *     none of the refusals a read of the configuration can, and what
         *     it reports is the slice its managers were built with rather than
         *     anything read back out of storage.
         *
         *     `async def`, unlike the repository's plain-`def` handlers, on
         *     purpose: the read then happens on the event loop that owns the
         *     managers, so it cannot interleave with a change to them and
         *     report half of one world.
         *
         *     An application built without a server around it has no runtime
         *     to report and answers with an empty object, which is the honesty
         *     `loaded_agents = ()` already has.
         */
        get: operations["read_mcp_server_status_runtime_mcp_servers_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/runtime/agents/{name}/prompt": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Agent Prompt
         * @description The system prompt a session opening now as this agent would
         *     be sent, block by block.
         *
         *     A runtime read rather than a database one: it is assembled from
         *     the agents this server loaded, the MCP slice its registry is
         *     running and the memory store it writes, so it cannot disagree
         *     with what a session would be given, which is the whole point of
         *     it.
         *
         *     It is a preview of a new session and says so, because there is
         *     no honest per-session answer to offer instead: a conversation
         *     already in progress holds the know-how half it assembled at its
         *     own activation, which may predate a reload, and what an operator
         *     audits is what the configuration produces now.
         *
         *     `async def`, like the status read beside it: the MCP slice is
         *     read on the loop that mutates it, so the answer cannot be half
         *     of one world and half of another. The memory read that follows
         *     is a file read and happens in a worker thread.
         *
         *     An agent this server did not load is a 404 naming the restart,
         *     since an agent is built at boot; an application with no server
         *     around it answers 503, like the reload.
         */
        get: operations["read_agent_prompt_runtime_agents__name__prompt_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/runtime/mcp-servers/reload": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reload Mcp Servers
         * @description Re-read the MCP servers and the agents' grants, and apply
         *     them to this running server.
         *
         *     The one action in this namespace, and the one exception to
         *     "configuration applies at the next start" that a request rather
         *     than a device asks for. What it re-reads is the `mcp_servers`
         *     entries, the secrets stored on them and the effective `mcp`
         *     lists, and nothing else: an agent's prompt, its providers, its
         *     memory and the whole server section stay as this process booted
         *     them, so a new agent still waits for the restart that builds it.
         *
         *     Nothing is stopped or started until every manager the new
         *     configuration needs has been built, so a refusal (422) has
         *     changed nothing at all. A server that merely will not connect is
         *     not a refusal: it applies, and says `down` with its reason
         *     below, and is reconnected in the background when a session that
         *     would use it opens.
         *
         *     Live conversations are not dropped, and when one meets the
         *     result depends on which half of an entry moved. The tools an
         *     agent may reach are snapshotted per reply, so a session picks
         *     those up on its next utterance; a call in flight on a server
         *     this stopped fails into the same error result a server dropping
         *     mid-call produces. An entry's `instructions` is prompt text,
         *     which is assembled at an activation and cached for it, so a
         *     rewrite reaches a conversation at its next activation, a new
         *     session or an agent switch. Such an entry is reported
         *     `unchanged` here, which is a statement about its connection: the
         *     connection is what did not change.
         *
         *     One reload runs at a time: a concurrent one is refused with 409
         *     and has changed nothing, like a write that could not take the
         *     database's lock.
         */
        post: operations["reload_mcp_servers_runtime_mcp_servers_reload_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/conversations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Conversations
         * @description The sessions this deployment recorded, newest first.
         *
         *     Filtered by `device` when given, and paginated with `cursor`,
         *     which holds the sessions below it, so a client walks back through
         *     the record one page at a time.
         */
        get: operations["read_conversations_conversations_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/conversations/{session}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Conversation
         * @description One session, whole: every column of its row, with how many
         *     turns and events hang off it.
         *
         *     The two counts are read in the same transaction as the row, so
         *     they describe one snapshot rather than three moments of a
         *     session that may still be running.
         */
        get: operations["read_conversation_conversations__session__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/conversations/{session}/turns": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read Conversation Turns
         * @description One session's timeline, oldest first, each turn carrying the
         *     calls it made.
         *
         *     `cursor` holds the turns strictly after it, which is the
         *     direction a client reconciling what it has already read asks in:
         *     the ids are monotonic and never reused, so what came after one is
         *     a stable question.
         */
        get: operations["read_conversation_turns_conversations__session__turns_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * Acknowledgement
         * @description What a write answers with: what it did, and when it takes
         *     effect.
         */
        Acknowledgement: {
            /**
             * Wrote
             * @description What was written or deleted, naming the entity the way the `vinga-server config` command names it in the line it prints.
             */
            wrote: string;
            /**
             * Notice
             * @description When the change takes effect, as one of three sentences. Configuration is a boot-time snapshot, so most writes apply at the next server start. A device binding and the default agent are read by the running server, so they apply at the device's next OTA check or connection with no restart, unless they name an agent this server has not loaded, which is the case that carries the restart sentence again. A write to an MCP server entry or to one of its secret slots names the reload instead, since that is what applies it to a running server.
             */
            notice: string;
        };
        /**
         * AssembledPrompt
         * @description The system prompt a session opening now as this agent would be
         *     sent.
         */
        AssembledPrompt: {
            /**
             * Blocks
             * @description The blocks in the order they are sent, joined by one blank line each: the persona, then the shared fragments the agent includes in the order its layer lists them, then the guidance of each MCP entry the agent is granted in grant order, and within one entry what its operator wrote, then what the server ships about itself, then the prompts it publishes in the order the entry names them, and then the remembered facts. The order is fixed and not configurable. A block that would hold nothing is not sent and is not listed, which is what an agent with no prompt of its own produces.
             */
            blocks: components["schemas"]["PromptBlock"][];
            /**
             * Characters
             * @description The whole prompt's length in characters, which is the sum of the blocks plus the blank line between each pair of them. The prompt is the blocks joined and nothing else, so a character counted here is a character the model receives.
             */
            characters: number;
        };
        /**
         * ConfigDocument
         * @description The whole domain configuration of one deployment, masked.
         */
        ConfigDocument: {
            /**
             * Config
             * @description The domain half of the configuration (providers, MCP servers, agent defaults, agents, devices, the default agent) in the shape `docs/reference/domain-config.md` documents, with every secret-bearing value masked.
             */
            config: {
                [key: string]: unknown;
            };
            /**
             * Secrets
             * @description Where every secret stored in the database is, which the masked document above cannot say. A list rather than a mapping, because a location is three fields and not a key.
             */
            secrets: components["schemas"]["StoredSecretLocation"][];
        };
        /**
         * ConversationDetail
         * @description One session, whole: its row and what hangs off it.
         */
        ConversationDetail: {
            /**
             * Id
             * @description The session's monotonic row id, the listing's cursor.
             */
            id: number;
            /**
             * Session
             * @description The session's uuid hex.
             */
            session: string;
            /**
             * Device
             * @description The device's MAC in canonical form, or null when none was understood.
             */
            device: string | null;
            /**
             * Client
             * @description The client identifier the device announced, when it announced one.
             */
            client: string | null;
            /**
             * Agent
             * @description The agent the session opened with, before any handover.
             */
            agent: string | null;
            /**
             * Agents
             * @description Every agent the device is bound to, by name, as the binding resolved at open. The first is the agent the session started on.
             */
            agents: string[] | null;
            /**
             * Protocol
             * @description The device protocol version this session negotiated.
             */
            protocol: string | null;
            /**
             * Started At
             * @description When the session opened, as an ISO-8601 instant in UTC.
             */
            started_at: string;
            /**
             * Closed At
             * @description When it closed, in the same form, or null.
             */
            closed_at: string | null;
            /**
             * Duration S
             * @description How long it lasted, in seconds. Null under metrics-off.
             */
            duration_s: number | null;
            /**
             * Close Reason
             * @description What ended it, one of the five tokens the listing describes, or null until it closes.
             */
            close_reason: ("limit" | "idle" | "drain" | "client" | "error") | string | null;
            /**
             * Server Version
             * @description The server version that recorded this session.
             */
            server_version: string | null;
            /**
             * Revision
             * @description The build revision that recorded it.
             */
            revision: string | null;
            /**
             * Providers
             * @description The resolved provider entry per pipeline stage, the same structure the capture manifest carries. It holds environment variable names, never credentials.
             */
            providers: {
                [key: string]: unknown;
            } | null;
            /**
             * Metrics
             * @description Whether metrics storage was on for this session, so a null number is distinguishable from a number that was never stored.
             */
            metrics: boolean;
            /**
             * Text
             * @description Whether text storage was on for this session, so a null utterance is distinguishable from an utterance that was never stored.
             */
            text: boolean;
            /**
             * Dropped
             * @description Records this session lost: events refused at the writer's in-flight bound, and anything a failed transaction rolled back. The store recording its own incompleteness, the way the capture manifest records `complete`.
             */
            dropped: number;
            /**
             * Turns
             * @description How many turns this session holds.
             */
            turns: number;
            /**
             * Events
             * @description How many events rows it holds: the decision track, `session_open` through `session_closed`. Zero under metrics-off, where no events row lands. They are deliberately not served over REST; the database is that surface.
             */
            events: number;
        };
        /**
         * ConversationList
         * @description One page of the session listing, newest first.
         */
        ConversationList: {
            /**
             * Items
             * @description The sessions on this page, newest first.
             */
            items: components["schemas"]["ConversationSummary"][];
            /**
             * Next Cursor
             * @description What to send as `cursor` for the page after this one, and null when this was the last: it is the id of the last item here, and the next page holds the sessions below it. Null means there is nothing further right now, not that there never will be; a listing re-read later starts from the top.
             */
            next_cursor: number | null;
        };
        /**
         * ConversationSummary
         * @description One session as the listing shows it.
         */
        ConversationSummary: {
            /**
             * Id
             * @description The session's monotonic row id, never reused. It is this listing's cursor: a page asked for with it holds the sessions before it.
             */
            id: number;
            /**
             * Session
             * @description The session's uuid hex, which addresses the two reads below and names the capture triplet of the same conversation.
             */
            session: string;
            /**
             * Device
             * @description The device's MAC in canonical form, and null when the session was rejected before one was understood.
             */
            device: string | null;
            /**
             * Agent
             * @description The agent the session opened with, before any handover.
             */
            agent: string | null;
            /**
             * Started At
             * @description When the session opened, as an ISO-8601 instant in UTC.
             */
            started_at: string;
            /**
             * Closed At
             * @description When it closed, in the same form. Null in a session that is still running and in one whose close was never persisted, which a crash leaves behind.
             */
            closed_at: string | null;
            /**
             * Duration S
             * @description How long it lasted, in seconds. A measured number: null under metrics-off.
             */
            duration_s: number | null;
            /**
             * Close Reason
             * @description What ended it, one of `limit`, `idle`, `drain`, `client` or `error`, the first cause to fire winning. Null until it closes. The five tokens are the set a server latches, and the column that holds them is deliberately unconstrained, so a token a later release adds is served as it was stored: a read that refused one would drop a whole page over one row, the same reason the database refuses none.
             */
            close_reason: ("limit" | "idle" | "drain" | "client" | "error") | string | null;
            /**
             * Turns
             * @description How many turns this session holds.
             */
            turns: number;
        };
        /**
         * ConversationTurn
         * @description One utterance and the reply it got, with the calls the reply made.
         */
        ConversationTurn: {
            /**
             * Id
             * @description The turn's monotonic row id, never reused. It is this timeline's cursor: a page asked for with it holds the turns after it.
             */
            id: number;
            /**
             * T Ms
             * @description The utterance's offset from session open, in milliseconds, aligned with its `heard` event and with the capture's audio for the same session.
             */
            t_ms: number;
            /**
             * Agent
             * @description The agent that answered, which a handover makes different from the session's.
             */
            agent: string | null;
            /**
             * Heard
             * @description What was said to the device, as transcribed. Null under text-off.
             */
            heard: string | null;
            /**
             * Heard Duration S
             * @description How long the utterance lasted, in seconds. Null under metrics-off.
             */
            heard_duration_s: number | null;
            /**
             * Language
             * @description The language the transcript was recognized as. Neither a measured number nor conversation text, so it survives both switches.
             */
            language: string | null;
            /**
             * Language Confidence
             * @description How sure the recognizer was of that language. Null under metrics-off.
             */
            language_confidence: number | null;
            /**
             * Reply
             * @description What the assistant said, the legs joined. Null under text-off, and null when the reply spoke nothing.
             */
            reply: string | null;
            /**
             * Legs
             * @description One entry per agent that took part, and present only when a handover split the reply. Null is a turn one agent answered whole, which is not the same as an empty list and never becomes one.
             */
            legs: components["schemas"]["TurnLeg"][] | null;
            /**
             * Asr Ms
             * @description Transcription elapsed, in milliseconds. Null where none was measured this turn, and under metrics-off.
             */
            asr_ms: number | null;
            /**
             * First Token Ms
             * @description Request to the reply's first token, in milliseconds. Null under metrics-off.
             */
            first_token_ms: number | null;
            /**
             * Llm Ms
             * @description The reply's LLM round durations summed, in milliseconds. Null under metrics-off.
             */
            llm_ms: number | null;
            /**
             * Tts First Audio Ms
             * @description The reply's first synthesis request to its first audio bytes, in milliseconds, measured at the provider boundary and deliberately not at the device. Null when the reply spoke nothing, and under metrics-off.
             */
            tts_first_audio_ms: number | null;
            /**
             * Rounds
             * @description How many LLM rounds the reply took. Null under metrics-off.
             */
            rounds: number | null;
            /**
             * Input Tokens
             * @description Input tokens summed across the turn's rounds; OTel's `gen_ai.usage.input_tokens`. Null when the provider reported no usage, and under metrics-off.
             */
            input_tokens: number | null;
            /**
             * Output Tokens
             * @description Output tokens summed across the turn's rounds; OTel's `gen_ai.usage.output_tokens`. Null when the provider reported no usage, and under metrics-off.
             */
            output_tokens: number | null;
            /**
             * Tool Calls
             * @description How many calls this turn issued, which is how many entries the list below holds. Structural rather than telemetry: it survives both switches.
             */
            tool_calls: number;
            /**
             * Tool Invocations
             * @description The calls the reply made, in the order the model issued them.
             */
            tool_invocations: components["schemas"]["ToolInvocation"][];
        };
        /**
         * ConversationTurns
         * @description One page of a session's timeline, oldest first.
         */
        ConversationTurns: {
            /**
             * Items
             * @description The turns on this page, ascending by id, which is chronological.
             */
            items: components["schemas"]["ConversationTurn"][];
            /**
             * Next Cursor
             * @description What to send as `cursor` for the page after this one, and null when this was the last. The next page holds the turns after it, which is also how a client that read up to a turn asks for what has happened since.
             */
            next_cursor: number | null;
        };
        /**
         * DefaultAgent
         * @description The agent an unbound device reaches.
         */
        DefaultAgent: {
            /**
             * Name
             * @description The default agent's name, or null when none is set, which leaves the devices map as the allowlist.
             */
            name: string | null;
        };
        /**
         * Envelope
         * @description One entity as a read returns it: the entity, and its stored-secret
         *     slots beside it.
         */
        Envelope: {
            /**
             * Entity
             * @description The entity's body, with every secret-bearing value masked, in the shape a write of it accepts and resubmittable as it stands. A PUT of it replaces the model-shaped half and never the credentials stored beside it; a field this read leaves out is one whose absence is what it means, and it means the same absence on the way back. An environment reference reads back as itself; a value shown as the mask, `********`, resubmits as keep the stored value, which is substituted before the fragment is validated, and that mask written where nothing is stored is refused. Described rather than validated here, because the mask is not a value the entity model would accept on its own: the entity schemas under `components/schemas` are what say which keys a write may carry.
             */
            entity: {
                [key: string]: unknown;
            };
            /**
             * Secrets
             * @description The slots holding a secret stored in the database, by slot name, and never their values: reads are masked. Empty for the kinds that can hold no stored secret (prompt fragments, agents, agent defaults, devices), so that every read has one shape. Display-only, and nothing to act on when resubmitting the entity above: a stored secret is left exactly as it is by a write of the entity, and rotating one is the secret PUT, which is the one door a plaintext value enters by.
             */
            secrets: {
                [key: string]: components["schemas"]["SecretSlot"];
            };
        };
        /**
         * McpReloadResult
         * @description What one reload did, and what is running once it had done it.
         *
         *     Both halves in one answer on purpose: the request that applies a
         *     change is the request that says what the change was and how it came
         *     out, so believing a write took effect when it did not takes a
         *     deliberate act of not reading the reply.
         */
        McpReloadResult: {
            /**
             * Started
             * @description The entries that had no connection before this reload and have one now: newly written, or newly named by some agent's `mcp` list. Started is not connected: an entry here whose server was unreachable is `down` below, with its reason.
             */
            started: string[];
            /**
             * Restarted
             * @description The entries whose fragment or whose stored secrets changed. Their connections were closed and made again, so a rotated credential applies here rather than at the next server start.
             */
            restarted: string[];
            /**
             * Stopped
             * @description The entries this server no longer connects: deleted, or no longer named by any agent. A deleted one is gone from the status below; a de-referenced one is still there, as `unused`.
             */
            stopped: string[];
            /**
             * Unchanged
             * @description The entries that kept the connection they had. It is a statement about the connection and not about the entry's text: an entry whose `instructions` was rewritten is here, because that field configures a prompt rather than a connection, and restarting a live connection to apply it would drop mid-call tools and respawn a stdio child for nothing. The conversations using such an entry keep the tools they had and the guidance they were activated with; the new guidance reaches them at their next activation.
             */
            unchanged: string[];
            /**
             * Servers
             * @description What every configured entry is doing now that the reload has been applied, keyed by entry name: exactly what `GET /runtime/mcp-servers` answers, taken in the same breath so that applying and verifying are one round trip.
             */
            servers: {
                [key: string]: components["schemas"]["McpServerStatus"];
            };
        };
        /**
         * McpServerStatus
         * @description One configured MCP server, as the running server sees it.
         *
         *     The listing is keyed by the entry's name, because that is what the
         *     operator wrote and what every tool the server publishes is prefixed
         *     with.
         */
        McpServerStatus: {
            /**
             * State
             * @description What this entry is doing: `connected` and offering the tools below, `down` and offering none, or `unused` because no agent references it, so this server never built a connection for it at all.
             * @enum {string}
             */
            state: "connected" | "down" | "unused";
            /**
             * Reason
             * @description Why a `down` server is down, as a fixed token this server owns: the class of the failure, or `DroppedAfterFailedCall` for a connection dropped after a tool call failed on it. Null when it is not down. Never a message the far side wrote, since an MCP server is a third party and its bytes are not this API's to publish.
             */
            reason: string | null;
            /**
             * Since
             * @description When this state was last entered, as an ISO-8601 instant in UTC. A new reason for staying down counts as entering it again, since it is a fresh failure. For an entry no agent references it is when the running configuration took effect.
             */
            since: string;
            /**
             * Tools
             * @description What this server published, under the names the model is given (`<entry>__<tool>`, sanitized). Empty while it is down. Only names cross this surface: a description, or the name a server listed before the publishing rule got to it, is bytes that server chose, and a server holding a credential of this deployment's could reflect it in either.
             */
            tools: string[];
            /**
             * Grants
             * @description Which agents may reach this server, by agent name. The value is how much of the server the agent gets: null is the whole server, and a list is the tools that agent was allowed, by the published name without the entry prefix. Beside the published list above it, so an allowed name this server does not offer is answerable in one read.
             */
            grants: {
                [key: string]: string[] | null;
            };
        };
        /**
         * PendingDevice
         * @description One device waiting to be claimed, as the listing shows it.
         *
         *     The listing is keyed by the code, because the code is what the
         *     operator has: they are holding a board with six digits on it, and
         *     the question the board model and the firmware version answer is
         *     which of these entries is that board.
         */
        PendingDevice: {
            /**
             * Mac
             * @description The device's MAC in canonical form, which is the row a successful claim writes.
             */
            mac: string;
            /**
             * Client Id
             * @description The UUID the device sent as its Client-Id at its last check-in.
             */
            client_id: string;
            /**
             * Board
             * @description The board type the device reported, such as waveshare-esp32-s3-touch-lcd-1.54, or `unknown` when it reported none. Whatever the device said, bounded in length and stripped of anything unprintable.
             */
            board: string;
            /**
             * Firmware
             * @description The firmware version the device reported, or 0.0.0 when it reported none.
             */
            firmware: string;
            /**
             * First Seen
             * @description When this device first checked in, as an ISO-8601 instant in UTC.
             */
            first_seen: string;
            /**
             * Last Seen
             * @description Its most recent check-in, in the same form.
             */
            last_seen: string;
            /**
             * Expires At
             * @description When this code stops being claimable. The device re-checks every couple of minutes and displays whatever the fresh reply carries, so an expired code is replaced on the screen rather than leaving the device stranded.
             */
            expires_at: string;
        };
        /**
         * PromptBlock
         * @description One block of an assembled system prompt.
         */
        PromptBlock: {
            /**
             * Provenance
             * @description Where this block came from, as a fixed token this server owns: `persona` for the agent's own prompt, `fragment:<name>` for a shared prompt fragment the agent includes, `instructions:<entry>` for the guidance written on an MCP server entry the agent is granted, `server_instructions:<entry>` for the guidance that server ships about itself where the entry opted into it, `server_prompt:<entry>:<position>` for one of the prompts it publishes, at its position in that entry's `inject_prompts` counted from one, `memory` for what the agent remembers. The fragment and entry names are the operator's, and both have been through the rule that keeps them to `[A-Za-z0-9_-]+`; a published prompt is identified by its position rather than by its name, because the name is a string the server chose and this token is printed in logs.
             */
            provenance: string;
            /**
             * Name
             * @description The name the entry's `inject_prompts` gave the published prompt this block came from, and null for every other kind of block. It is carried here and not in the provenance because it is a server-chosen string the operator copied into their configuration, so nothing bounds what it holds; this body is JSON-encoded and is one of the two places operator-written configuration is echoed back, which the tokens printed in logs and structured events are not.
             */
            name?: string | null;
            /**
             * Characters
             * @description How long this block is, in characters, counted on what is stored and sent. It is what an operator tunes a small model's context budget against, block by block.
             */
            characters: number;
            /**
             * Text
             * @description The block as the model receives it, heading included, whoever wrote it: a surface that hid part of the prompt would fail its own purpose, which is to say what the model was given, and an entry that opted into a server's own guidance opted those bytes into this prompt. The provenance beside it is what says whose words they are.
             */
            text: string;
        };
        /**
         * SecretSlot
         * @description One slot of an entity that holds a secret stored in the database.
         */
        SecretSlot: {
            /**
             * Shadows
             * @description The entity key this stored secret displaces, or null when the entity writes no reference for the slot. A stored secret takes precedence over an environment reference written for the same slot, and this names what it takes the place of.
             */
            shadows: string | null;
        };
        /**
         * StoredSecretLocation
         * @description Where one stored secret is, in the whole-configuration read.
         */
        StoredSecretLocation: {
            /**
             * Kind
             * @description The kind of entity holding it: provider or mcp_server.
             */
            kind: string;
            /**
             * Identity
             * @description The entity's identity: `<stage>.<name>` for a provider, the name for an MCP server.
             */
            identity: string;
            /**
             * Slot
             * @description The credential slot inside that entity.
             */
            slot: string;
            /**
             * Shadows
             * @description The entity key this stored secret displaces, or null.
             */
            shadows: string | null;
        };
        /**
         * ToolInvocation
         * @description One call a turn issued, as the timeline nests it.
         *
         *     The transport shape of a `tool_invocations` row, not the record the
         *     pipeline hands the writer, which is the dataclass of the same name
         *     in `records.py`.
         */
        ToolInvocation: {
            /**
             * Position
             * @description Where this call sat in the round's call list, as the model issued it, counted from zero and with handovers included. The rows are nested in this order; it is the model's order and not the order they finished in.
             */
            position: number;
            /**
             * Source
             * @description Where the call was routed: `builtin` for one this application authors, `device` for one the device published, `mcp` for one an MCP entry owns, `unknown` for a name nothing answered to. Classified before the call ran. A closed set here because it is a closed set in the database, which holds the same four tokens under a check constraint.
             * @enum {string}
             */
            source: "builtin" | "device" | "mcp" | "unknown";
            /**
             * Entry
             * @description The owning MCP entry's configured name for an `mcp` call, and null otherwise. A name this deployment chose, so it survives text-off.
             */
            entry: string | null;
            /**
             * Name
             * @description The called tool's name, and null when text storage was off for the session: a tool's name originates off this server, a device's self-description or an MCP far side, exactly as its result does.
             */
            name: string | null;
            /**
             * Malformed
             * @description Whether the model's arguments were not a JSON object.
             */
            malformed: boolean;
            /**
             * Arguments
             * @description What the model passed, null under text-off and null when the call was malformed, which is what `malformed` above tells them apart by.
             */
            arguments: {
                [key: string]: unknown;
            } | null;
            /**
             * Result
             * @description What the call answered, a refusal included. Null under text-off.
             */
            result: string | null;
            /**
             * Is Error
             * @description Whether the call answered as an error.
             */
            is_error: boolean;
            /**
             * Duration Ms
             * @description How long the call took, in milliseconds. Null where nothing ran, as for a refused or a successful handover, and null under metrics-off.
             */
            duration_ms: number | null;
        };
        /**
         * TurnLeg
         * @description One agent's share of a turn a handover split.
         *
         *     The transport shape of one entry of `turns.legs`, whose halves
         *     follow different storage switches: the text is content and the token
         *     counts are measurements, which is why a leg exists at all rather
         *     than the turn's totals being the whole story. The totals blend
         *     agents that may use different models; this is where they come apart
         *     again.
         */
        TurnLeg: {
            /**
             * Agent
             * @description The agent whose leg this is, and null for a session that never activated one.
             */
            agent: string | null;
            /**
             * Text
             * @description What this agent said, null under text-off, and null for an agent that took part without speaking: one that asked for the handover said nothing and spent tokens all the same.
             */
            text: string | null;
            /**
             * Input Tokens
             * @description Input tokens this agent spent on the turn. Null when the provider reported no usage, and under metrics-off.
             */
            input_tokens: number | null;
            /**
             * Output Tokens
             * @description Output tokens this agent spent on the turn. Null when the provider reported no usage, and under metrics-off.
             */
            output_tokens: number | null;
        };
        /**
         * ProviderConfig
         * @description One provider entry. Options beyond `type` are passed through to the
         *     provider implementation, so extra keys are allowed here.
         */
        ProviderConfig: {
            /**
             * Type
             * @description The provider implementation this entry configures, such as anthropic, openai_compatible, faster_whisper, openai, piper, elevenlabs or silero. Every key beyond the ones listed here is an option for that implementation.
             */
            type: string;
            /**
             * Api Key Env
             * @description The name of the environment variable holding this provider's credential, never the credential itself. Left unset for a local engine or a keyless self-hosted endpoint. A credential stored with `config set-secret` fills the same slot and takes precedence.
             * @default null
             */
            api_key_env?: string | null;
            /**
             * Egress
             * @description Whether this entry sends session data off the host, asserted by the operator for the types whose configuration decides it rather than their name (openai_compatible, and the openai ASR and TTS types, whose base_url may be local or a vendor). Under server.local_only such an entry must declare egress: false; a type that knows its own egress rejects the key.
             * @default null
             */
            egress?: boolean | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * McpServerConfig
         * @description One MCP server, named so agents can reference it.
         *
         *     `transport` decides which of the two field groups applies: a stdio
         *     server is a command this server spawns, a streamable_http one is a
         *     URL it connects to. Naming a field of the other transport is an
         *     error rather than a silently ignored key, since the difference
         *     between "my headers are ignored" and "my headers are wrong" is a
         *     debugging afternoon.
         */
        McpServerConfig: {
            /**
             * Transport
             * @description Which field group applies: stdio spawns `command` as a subprocess, streamable_http connects to `url`. Naming a field of the other transport is an error rather than a silently ignored key. An SSE-only endpoint is configured as a stdio server behind an mcp-proxy bridge, since the specification deprecated SSE in favour of streamable_http and there is no native arm for it here.
             * @enum {string}
             */
            transport: "stdio" | "streamable_http";
            /**
             * Command
             * @description The executable a stdio server is spawned as. Required for that transport, and rejected for streamable_http.
             * @default null
             */
            command?: string | null;
            /**
             * Args
             * @description The arguments the stdio command is spawned with, one per entry.
             */
            args?: string[];
            /**
             * Env
             * @description Environment variables for the spawned stdio command. A value of $NAME is read from the server's own environment at startup, and any secret-bearing key (token, api_key, authorization, ...) must use that form.
             */
            env?: {
                [key: string]: string;
            };
            /**
             * Url
             * @description The endpoint a streamable_http server is reached at. Required for that transport, and rejected for stdio.
             * @default null
             */
            url?: string | null;
            /**
             * Headers
             * @description Headers sent with every streamable_http request. A value of $NAME is read from the server's own environment at startup, and any secret-bearing key must use that form.
             */
            headers?: {
                [key: string]: string;
            };
            /**
             * Egress
             * @description Whether this server sends session data off the local network. Tool arguments carry conversation-derived data and neither transport can tell on its own, since a stdio command may proxy anywhere and a url may name localhost, so under server.local_only every referenced entry must declare it.
             * @default null
             */
            egress?: boolean | null;
            /**
             * Tool Timeout S
             * @description How long one tool call on this server may take, in seconds, before the model is told it timed out. The device hears silence meanwhile, so keep it short.
             * @default 15
             */
            tool_timeout_s?: number;
            /**
             * Instructions
             * @description Guidance for the model about using this server's tools, injected into the system prompt of every agent this entry is granted to, under a heading naming the prefix its tools carry. It is written for the whole entry rather than per tool, so guidance about a tool an agent's allow list withholds is noise the operator avoids by writing about the granted surface. The grant is the whole condition: it is injected whether or not the server is connected and whatever its tools turn out to be, and an agent with `mcp: []` never sees it. It is stored and injected as written, its indentation and its own blank lines included; the only bytes trimmed are whitespace at the two ends of the whole assembled prompt, which is also what the inspection surface reports. Editing it does not restart the connection, so a reload reports the entry as `unchanged`, and the new text reaches a conversation at its next activation: a new session or an agent switch, never a reply of a session already running.
             * @default null
             */
            instructions?: string | null;
            /**
             * Use Server Instructions
             * @description Whether to inject the guidance this server ships about itself, the `instructions` field of its initialize result, into the system prompt of every agent this entry is granted to. Off by default, and deliberately: the entry's own `instructions` is what your operator wrote, while this is a third party's text steering the agent, so consuming it is an explicit opt-in taken per entry and per channel. What a server ships is captured on every connect whatever this says, so turning it on applies at the next reload without restarting the connection, and turning it off stops the injection at the next activation. A block longer than 4000 characters is skipped whole rather than truncated. The block sits after this entry's own guidance, and `vinga-server config prompt <agent>` reports it under `server_instructions:<entry>`, so an operator can see whose words they are reading.
             * @default false
             */
            use_server_instructions?: boolean;
            /**
             * Inject Prompts
             * @description The prompts this server publishes that are injected into the system prompt of every agent this entry is granted to, each by the name the server lists it under and in the order listed here. Unset means none, which is the default: a third party's text steering the agent is an opt-in per entry and per channel, and the specification defines prompts as user-controlled templates, so the operator who read the server's documentation names the ones that are standing guidance rather than invocable templates. Every name is validated against the server's own paginated prompt listing before anything is fetched, and a name the listing does not carry, one whose prompt declares required arguments, one that renders anything but text, and a rendered block longer than 4000 characters are each skipped with a warning naming this entry and the position in this list, never the name itself, since a server-chosen name is not this server's to print. Editing this list changes what a connect fetches, so unlike the other two prompt fields it restarts the connection when a reload applies it. A name listed twice is refused. Each name is stored and looked up exactly as written, surrounding whitespace included, because it is an identifier the server chose rather than a word this server may tidy: a stripped copy of it addresses a different prompt or none at all.
             * @default null
             */
            inject_prompts?: string[] | null;
        };
        /**
         * PromptFragmentConfig
         * @description One named block of prompt text, shared by the agents that include
         *     it.
         *
         *     A mapping with one field rather than a bare string. Every entity in
         *     this configuration travels the same path (a stored row, a read
         *     envelope, a written fragment), and all three of those want a mapping;
         *     a one-field mapping also leaves room for a second field later without
         *     changing the shape a client writes, which is why a grant took its
         *     object form too.
         */
        PromptFragmentConfig: {
            /**
             * Text
             * @description The text injected into the system prompt of every agent whose prompt_includes names this fragment, as written: its indentation and its own blank lines are part of it, and nothing is added around it, not even a heading, since this is prompt text the operator wrote and a heading would editorialize. The only bytes trimmed are whitespace at the two ends of the whole assembled prompt, which is also what the inspection surface reports. It sits after the agent's own prompt and before any MCP guidance, in the order the including layer lists it. There is no length cap: what each block costs is reported by `vinga-server config prompt <agent>`, and the operator is the one who knows what their model tolerates.
             */
            text: string;
        };
        /**
         * FillerConfig
         * @description Masking reply latency with a pre-synthesized filled pause.
         *
         *     Off by default. When enabled, the phrases are synthesized in the
         *     agent's own voice at boot and cached as PCM; a reply whose first
         *     audio has not started within `delay_ms` of the utterance being
         *     transcribed plays one, and the real reply queues behind its tail.
         *     A synthesis failure at boot logs a warning and leaves the feature
         *     off for that agent rather than failing the boot.
         */
        FillerConfig: {
            /**
             * Enabled
             * @description Whether a filled pause is played while a slow reply is prepared. Off by default.
             * @default false
             */
            enabled?: boolean;
            /**
             * Delay Ms
             * @description How long the user hears silence before the filler starts, in milliseconds, counted from the transcription of their utterance.
             * @default 1800
             */
            delay_ms?: number;
            /**
             * Phrases
             * @description The phrases to play, written in the agent's own language; the player rotates through them rather than always playing the same one. At least one is required when the feature is enabled.
             */
            phrases?: string[];
        };
        /**
         * McpGrant
         * @description One `mcp` list entry in its object form: a server, and which of
         *     its tools the layer may reach.
         *
         *     A second spelling of the same list entry rather than a different
         *     thing. A plain string names the whole server, and this names part of
         *     one, so an agent that should switch the lights but not unlock the
         *     door does not need a second server to say it in.
         *
         *     Tools are named by the published name without its entry prefix
         *     (`turn_on_light` grants `home__turn_on_light`), matched exactly. That
         *     identifier is this application's own: it has been through the
         *     publishing rule, it is what `vinga-server config status` shows and
         *     what the model calls, so an operator writes down the name they read.
         *     What a server listed before the rule got to it never appears on a
         *     vinga surface, and cannot be granted by.
         */
        McpGrant: {
            /**
             * Server
             * @description The MCP server this grant is about, by the name it is defined under in mcp_servers.
             */
            server: string;
            /**
             * Tools
             * @description Which of that server's tools this layer may reach, by the published name without its entry prefix (turn_on_light for home__turn_on_light), which is the name `vinga-server config status` shows. Leaving it out grants the whole server, exactly as naming the server as a plain string does. A name that matches nothing the server published is not an error at write time, since only a live connection knows the list; it is logged when the server publishes and visible under grants on the status surface.
             * @default null
             */
            tools?: string[] | null;
        };
        /**
         * AgentConfig
         * @description One agent: a prompt, plus whichever stages it overrides.
         */
        AgentConfig: {
            /**
             * Llm
             * @description The language model, by the name it is defined under in providers.llm. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            llm?: string | null;
            /**
             * Asr
             * @description The speech recognizer, by the name it is defined under in providers.asr. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            asr?: string | null;
            /**
             * Tts
             * @description The voice, by the name it is defined under in providers.tts. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            tts?: string | null;
            /**
             * Vad
             * @description The voice activity detector, by the name it is defined under in providers.vad. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            vad?: string | null;
            /**
             * Mcp
             * @description The MCP servers whose tools this layer offers the model. An entry is either the entry name on its own, which is the whole server, or an object naming the server and the tools of it this layer may reach ({server: home, tools: [turn_on_light]}), where a tool is named by its published name without the entry prefix. Unset inherits the agent_defaults list; naming a list replaces the inherited one rather than extending it, so an empty list opts an agent out of the tools its siblings have. The builtin tools are outside this model: switch_agent and remember appear under a structural condition (a device bound to more than one agent, memory configured) and random_number under none at all, rather than by grant.
             * @default null
             */
            mcp?: (string | components["schemas"]["McpGrant"])[] | null;
            /**
             * @description Latency masking with a pre-synthesized filled pause. Unset inherits the agent_defaults section; naming one replaces it wholly rather than merging with it, so `filler: {enabled: false}` opts an agent out.
             * @default null
             */
            filler?: components["schemas"]["FillerConfig"] | null;
            /**
             * Prompt Includes
             * @description The shared prompt fragments this layer's system prompt carries, each by the name it is defined under in prompt_fragments, injected in the order listed and directly after the agent's own prompt. Unset inherits the agent_defaults list; naming a list replaces the inherited one rather than extending it, so an empty list opts an agent out of the fragments its siblings share. Every name has to be a fragment that exists, since the fragment is in this same database, and a name listed twice is refused. Fragments are part of the boot-time snapshot, so a change here reaches a conversation at the next server start rather than at a reload.
             * @default null
             */
            prompt_includes?: string[] | null;
            /**
             * Prompt
             * @description The instruction this agent replies under, sent as the system prompt on every turn. State the reply language explicitly: a model otherwise picks one by its training bias.
             * @default
             */
            prompt?: string;
        };
        /**
         * AgentDefaults
         * @description Provider references every agent inherits unless it names its own.
         *
         *     Deliberately no prompt: an agent's prompt is its identity, and
         *     inheriting one silently would make two agents the same agent.
         */
        AgentDefaults: {
            /**
             * Llm
             * @description The language model, by the name it is defined under in providers.llm. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            llm?: string | null;
            /**
             * Asr
             * @description The speech recognizer, by the name it is defined under in providers.asr. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            asr?: string | null;
            /**
             * Tts
             * @description The voice, by the name it is defined under in providers.tts. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            tts?: string | null;
            /**
             * Vad
             * @description The voice activity detector, by the name it is defined under in providers.vad. An agent that leaves it unset inherits the agent_defaults entry.
             * @default null
             */
            vad?: string | null;
            /**
             * Mcp
             * @description The MCP servers whose tools this layer offers the model. An entry is either the entry name on its own, which is the whole server, or an object naming the server and the tools of it this layer may reach ({server: home, tools: [turn_on_light]}), where a tool is named by its published name without the entry prefix. Unset inherits the agent_defaults list; naming a list replaces the inherited one rather than extending it, so an empty list opts an agent out of the tools its siblings have. The builtin tools are outside this model: switch_agent and remember appear under a structural condition (a device bound to more than one agent, memory configured) and random_number under none at all, rather than by grant.
             * @default null
             */
            mcp?: (string | components["schemas"]["McpGrant"])[] | null;
            /**
             * @description Latency masking with a pre-synthesized filled pause. Unset inherits the agent_defaults section; naming one replaces it wholly rather than merging with it, so `filler: {enabled: false}` opts an agent out.
             * @default null
             */
            filler?: components["schemas"]["FillerConfig"] | null;
            /**
             * Prompt Includes
             * @description The shared prompt fragments this layer's system prompt carries, each by the name it is defined under in prompt_fragments, injected in the order listed and directly after the agent's own prompt. Unset inherits the agent_defaults list; naming a list replaces the inherited one rather than extending it, so an empty list opts an agent out of the fragments its siblings share. Every name has to be a fragment that exists, since the fragment is in this same database, and a name listed twice is refused. Fragments are part of the boot-time snapshot, so a change here reaches a conversation at the next server start rather than at a reload.
             * @default null
             */
            prompt_includes?: string[] | null;
        };
        /**
         * DeviceBinding
         * @description What a device write carries: the agents the device may reach.
         */
        DeviceBinding: {
            /**
             * Agents
             * @description The agents this device is bound to, by name. The first is the agent a conversation starts on and the rest are the ones switch_agent can reach. Every name has to be an agent that exists, or the write is refused.
             */
            agents: string[];
        };
        /**
         * DefaultAgentName
         * @description What a default-agent write carries. Clearing it is the DELETE,
         *     not a null here: one way to say a thing.
         */
        DefaultAgentName: {
            /**
             * Name
             * @description The agent an unbound device reaches. It has to be an agent that exists. To unset it, DELETE this resource, which leaves the devices map as the allowlist.
             */
            name: string;
        };
        /**
         * SecretValue
         * @description What a secret write carries: the credential itself, the only
         *     plaintext this API ever accepts.
         */
        SecretValue: {
            /**
             * Secret
             * @description The credential, in plaintext, stored encrypted under the newest key in VINGA_MASTER_KEY. It crosses the connection as itself, which is why the whole API belongs on a loopback connection or behind TLS. It is never read back: a read names the slot and masks the value. It may not be empty.
             */
            secret: string;
        };
        /**
         * FieldError
         * @description One field of the submitted body that a refusal names.
         */
        FieldError: {
            /**
             * Path
             * @description Which part of the submitted body this is about, as an RFC 6901 JSON Pointer into it: `/filler/phrases` is that field of that object, `/mcp/0` is that position of that list, and the empty string is the body as a whole. Only names this server declares and positions appear in it. A key the request invented is never one of them: an unrecognized key, an option a provider passes through, an entry of an `env` or `headers` map. A key is as good a place to paste a credential as a value is, so the pointer stops at the nearest enclosing place this server can name, which for a key written at the top of a fragment is the fragment itself.
             */
            path: string;
            /**
             * Message
             * @description What is wrong with it, in the same words the corresponding line of `detail` uses: the two are rendered from one computation, so a client showing this beside the field and one showing the whole sentence say the same thing. Like `detail`, it names a rule and never quotes the value it rejected or a key the request invented. Where the rule is about such a key, it names what made the key match instead, which is a word from a closed list this server owns.
             */
            message: string;
        };
        /**
         * Problem
         * @description A refusal, as RFC 9457 problem details.
         *
         *     Served as `application/problem+json`. `type` and `instance` are
         *     deliberately absent: an absent `type` means `about:blank`, which is
         *     the truth here, since these problems are described by their status
         *     and their prose rather than by a URI registry nobody serves.
         */
        Problem: {
            /**
             * Title
             * @description The status code's standard HTTP reason phrase, such as `Not Found`. It is the phrase and not a description of this API's own, because with `type` absent the problem type is `about:blank`, whose title RFC 9457 says should be the status's recommended phrase; a title of this server's own would imply a problem type the body does not identify. What was actually refused is `detail`.
             */
            title: string;
            /**
             * Status
             * @description The HTTP status code, repeated in the body as the RFC describes, so that a problem separated from its response (in a log, in a bug report) still says what it was answered under.
             */
            status: number;
            /**
             * Detail
             * @description What was refused and why, the same sentence the `vinga-server config` command prints for it. It names the entity the request addressed and the rule that was broken; it never quotes a secret, a configuration value that was rejected, or a key the request invented.
             */
            detail: string;
            /**
             * Errors
             * @description The fields of the submitted body the refusal names, one entry each, for a client that wants to mark the offending field rather than show a paragraph. Present on every refusal and empty where there is no field to name: a body whose whole shape was wrong, a reference to another entity, a failure that was not the caller's. The entries are the same problems `detail` lists, in the same order and the same words.
             */
            errors: components["schemas"]["FieldError"][];
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    read_config_config_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ConfigDocument"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_providers_providers_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: {
                            [key: string]: components["schemas"]["Envelope"];
                        };
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_provider_providers__stage___name__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                stage: string;
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Envelope"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_provider_providers__stage___name__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                stage: string;
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProviderConfig"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_provider_providers__stage___name__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                stage: string;
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_mcp_servers_mcp_servers_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: components["schemas"]["Envelope"];
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_mcp_server_mcp_servers__name__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Envelope"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_mcp_server_mcp_servers__name__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["McpServerConfig"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_mcp_server_mcp_servers__name__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_prompt_fragments_prompt_fragments_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: components["schemas"]["Envelope"];
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_prompt_fragment_prompt_fragments__name__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Envelope"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_prompt_fragment_prompt_fragments__name__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PromptFragmentConfig"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_prompt_fragment_prompt_fragments__name__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_agents_agents_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: components["schemas"]["Envelope"];
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_agent_agents__name__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Envelope"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_agent_agents__name__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AgentConfig"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_agent_agents__name__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_agent_defaults_agent_defaults_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Envelope"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_agent_defaults_agent_defaults_put: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AgentDefaults"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_devices_devices_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: components["schemas"]["Envelope"];
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_pending_devices_devices_pending_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: components["schemas"]["PendingDevice"];
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_device_devices__mac__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                mac: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Envelope"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_device_devices__mac__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                mac: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DeviceBinding"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_device_devices__mac__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                mac: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_default_agent_default_agent_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DefaultAgent"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_default_agent_default_agent_put: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DefaultAgentName"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_default_agent_default_agent_delete: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_provider_secret_providers__stage___name__secrets__slot__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                stage: string;
                name: string;
                slot: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SecretValue"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_provider_secret_providers__stage___name__secrets__slot__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                stage: string;
                name: string;
                slot: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    write_mcp_secret_mcp_servers__name__secrets__slot__put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
                slot: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SecretValue"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    remove_mcp_secret_mcp_servers__name__secrets__slot__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
                slot: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    add_device_devices_pending__code__post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                code: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DeviceBinding"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Acknowledgement"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Nothing of that identity exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request names something that cannot be addressed, such as a stage that is not a provider stage or a MAC address that is not one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_mcp_server_status_runtime_mcp_servers_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: components["schemas"]["McpServerStatus"];
                    };
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_agent_prompt_runtime_agents__name__prompt_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssembledPrompt"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description No agent of that name was loaded when this server started. An agent written since then waits for the restart that builds its providers. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request could not be read in the shape this endpoint expects. The refusal is the sanitized `Problem` body every other refusal uses, and nothing sent is quoted back. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description This application has no running server around it, so there is no loaded agent, no running MCP slice and no memory store to assemble a prompt from. Unlike the MCP status read beside it, there is no honest empty answer: an empty block list would say a session opening now is sent nothing. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    reload_mcp_servers_runtime_mcp_servers_reload_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpReloadResult"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Something this request needs is held by another one: the configuration database's write lock, the activation code a concurrent claim is already binding, or a reload of the MCP servers that is already running. Nothing was changed and the request can be retried. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration was refused: it does not compose into a valid snapshot, or a server it names could not be built (an environment reference nothing sets, a stored credential that will not decrypt, an entry `server.local_only` forbids). Nothing was stopped, started or swapped, and the running servers are exactly as they were. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The stored configuration cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description This application has no running server around it, so there is nothing for a runtime action to act on. A deployment reaches the API on its server's own port; an application built without one serves the reads in the /runtime namespace emptily and refuses the actions. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_conversations_conversations_get: {
        parameters: {
            query?: {
                /** @description Only the sessions of this device, by MAC. Colons or dashes, upper or lower case; it is normalized before it is matched. Anything that is not a MAC address is refused. */
                device?: string | null;
                /** @description How many rows this page may hold: a whole number from 1 to 200, defaulting to 50. Anything else is refused. */
                limit?: string | null;
                /** @description Where to carry on from: a row id this API answered with, as a whole number. Absent means the first page. Anything else is refused. */
                cursor?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ConversationList"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description There is no conversation store to read, or no session of that id in it. The body says which. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description One of the query arguments could not be read: the limit, the cursor or the device filter. Nothing sent is quoted back. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The conversation store cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_conversation_conversations__session__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ConversationDetail"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description There is no conversation store to read, or no session of that id in it. The body says which. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description One of the query arguments could not be read: the limit, the cursor or the device filter. Nothing sent is quoted back. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The conversation store cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    read_conversation_turns_conversations__session__turns_get: {
        parameters: {
            query?: {
                /** @description How many rows this page may hold: a whole number from 1 to 200, defaulting to 50. Anything else is refused. */
                limit?: string | null;
                /** @description Where to carry on from: a row id this API answered with, as a whole number. Absent means the first page. Anything else is refused. */
                cursor?: string | null;
            };
            header?: never;
            path: {
                session: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ConversationTurns"];
                };
            };
            /** @description The request carried no bearer token, or not the one this server was given. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description There is no conversation store to read, or no session of that id in it. The body says which. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description One of the query arguments could not be read: the limit, the cursor or the device filter. Nothing sent is quoted back. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The conversation store cannot be read, or the request failed for a reason that is not the caller's. The details are in the server's log. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
}
