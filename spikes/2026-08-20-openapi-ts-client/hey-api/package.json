{
  "name": "vinga-spike-hey-api",
  "version": "0.0.0",
  "private": true,
  "description": "Spike sub-project: generate a TypeScript client for the vinga configuration API with Hey API's openapi-ts, and type-check a consumer fixture against it.",
  "type": "module",
  "scripts": {
    "generate": "openapi-ts",
    "typecheck": "tsc --noEmit",
    "probe": "tsx probe.ts",
    "check": "npm run typecheck && npm run probe"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "0.99.0",
    "tsx": "4.23.12",
    "typescript": "5.9.3"
  }
}
