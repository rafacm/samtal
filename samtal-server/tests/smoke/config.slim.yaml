# The server half of the slim image's boot check.
#
# Its domain half is seeded by seed-slim.sh, where every network stage
# names an external provider, which is the whole point of the slim
# variant: that configuration loads no local engine, so it has to come
# up in an image that carries none.

server:
  host: 0.0.0.0
  port: 8003
